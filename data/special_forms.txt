# GENERATED by scripts/check_special_form_enum.sh --write — the exact set
# of `SpecialFormKind` variants in src/eval/analyzer/analyzer.zig.
#
# ADR-0004 locks this enum's shape; adding or removing a name here is an
# amendment to that ADR, not a formality. The gate asserts the code still
# matches this file, so neither side can drift without the other.
binding_form
def
defmacro_form
do_form
dot_form
fn_star
if_form
in_ns_form
let_star
letfn_star
loop_star
mut_fields
new_form
ns_form
quote_form
recur_form
require_form
set_bang
throw_form
try_form
var_form
