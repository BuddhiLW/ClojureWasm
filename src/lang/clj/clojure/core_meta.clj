;; SPDX-License-Identifier: EPL-2.0
;;
;;   Copyright (c) Rich Hickey. All rights reserved.
;;   The use and distribution terms for this software are covered by the
;;   Eclipse Public License 1.0 (http://opensource.org/licenses/eclipse-1.0.php).
;;   By using this software in any fashion, you are agreeing to be bound by the
;;   terms of this license. You must not remove this notice, or any other, from
;;   this software.
;;
;;   core_meta.clj — :arglists/:doc for clojure.core vars; docstring text
;;   reproduced verbatim from Clojure's core sources. Reproduced in
;;   ClojureWasm; redistributed under EPL-2.0 per EPL-1.0 §7. ClojureWasm
;;   changes (c) the ClojureWasm authors.
;;   GENERATED by scripts/extract_core_meta.sh — do not hand-edit.

(ns clojure.core-meta)

;; Existing keys win: a var's own meta (a defn-attached :arglists) is
;; preserved; only the missing keys are filled from the oracle table.
(def ^:private fill (fn [m mm] (merge mm m)))

(when-let [v (resolve (quote clojure.core/*))] (alter-meta! v fill {:arglists (quote ([] [x] [x y] [x y & more])), :doc "Returns the product of nums. (*) returns 1. Does not auto-promote\n  longs, will throw on overflow. See also: *'"}))
(when-let [v (resolve (quote clojure.core/*'))] (alter-meta! v fill {:arglists (quote ([] [x] [x y] [x y & more])), :doc "Returns the product of nums. (*') returns 1. Supports arbitrary precision.\n  See also: *"}))
(when-let [v (resolve (quote clojure.core/*1))] (alter-meta! v fill {:doc "bound in a repl thread to the most recent value printed"}))
(when-let [v (resolve (quote clojure.core/*2))] (alter-meta! v fill {:doc "bound in a repl thread to the second most recent value printed"}))
(when-let [v (resolve (quote clojure.core/*3))] (alter-meta! v fill {:doc "bound in a repl thread to the third most recent value printed"}))
(when-let [v (resolve (quote clojure.core/*agent*))] (alter-meta! v fill {:doc "The agent currently running an action on this thread, else nil"}))
(when-let [v (resolve (quote clojure.core/*assert*))] (alter-meta! v fill {:doc "When set to logical false, 'assert' will omit assertion checks in\n  compiled code. Defaults to true."}))
(when-let [v (resolve (quote clojure.core/*clojure-version*))] (alter-meta! v fill {:doc "The version info for Clojure core, as a map containing :major :minor \n  :incremental and :qualifier keys. Feature releases may increment \n  :minor and/or :major, bugfix releases will increment :incremental. \n  Possible values of :qualifier include \"GA\", \"SNAPSHOT\", \"RC-x\" \"BETA-x\""}))
(when-let [v (resolve (quote clojure.core/*command-line-args*))] (alter-meta! v fill {:doc "A sequence of the supplied command line arguments, or nil if\n  none were supplied"}))
(when-let [v (resolve (quote clojure.core/*data-readers*))] (alter-meta! v fill {:doc "Map from reader tag symbols to data reader Vars.\n\n  When Clojure starts, it searches for files named 'data_readers.clj'\n  and 'data_readers.cljc' at the root of the classpath. Each such file\n  must contain a literal map of symbols, like this:\n\n      {foo/bar my.project.foo/bar\n       foo/baz my.project/baz}\n\n  The first symbol in each pair is a tag that will be recognized by\n  the Clojure reader. The second symbol in the pair is the\n  fully-qualified name of a Var which will be invoked by the reader to\n  parse the form following the tag. For example, given the\n  data_readers.clj file above, the Clojure reader would parse this\n  form:\n\n      #foo/bar [1 2 3]\n\n  by invoking the Var #'my.project.foo/bar on the vector [1 2 3]. The\n  data reader function is invoked on the form AFTER it has been read\n  as a normal Clojure data structure by the reader.\n\n  Reader tags without namespace qualifiers are reserved for\n  Clojure. Default reader tags are defined in\n  clojure.core/default-data-readers but may be overridden in\n  data_readers.clj, data_readers.cljc, or by rebinding this Var."}))
(when-let [v (resolve (quote clojure.core/*default-data-reader-fn*))] (alter-meta! v fill {:doc "When no data reader is found for a tag and *default-data-reader-fn*\n  is non-nil, it will be called with two arguments,\n  the tag and the value.  If *default-data-reader-fn* is nil (the\n  default), an exception will be thrown for the unknown tag."}))
(when-let [v (resolve (quote clojure.core/*e))] (alter-meta! v fill {:doc "bound in a repl thread to the most recent exception caught by the repl"}))
(when-let [v (resolve (quote clojure.core/*err*))] (alter-meta! v fill {:doc "A java.io.Writer object representing standard error for print operations.\n\n  Defaults to System/err, wrapped in a PrintWriter"}))
(when-let [v (resolve (quote clojure.core/*file*))] (alter-meta! v fill {:doc "The path of the file being evaluated, as a String.\n\n  When there is no file, e.g. in the REPL, the value is not defined."}))
(when-let [v (resolve (quote clojure.core/*flush-on-newline*))] (alter-meta! v fill {:doc "When set to true, output will be flushed whenever a newline is printed.\n\n  Defaults to true."}))
(when-let [v (resolve (quote clojure.core/*in*))] (alter-meta! v fill {:doc "A java.io.Reader object representing standard input for read operations.\n\n  Defaults to System/in, wrapped in a LineNumberingPushbackReader"}))
(when-let [v (resolve (quote clojure.core/*ns*))] (alter-meta! v fill {:doc "A clojure.lang.Namespace object representing the current namespace."}))
(when-let [v (resolve (quote clojure.core/*out*))] (alter-meta! v fill {:doc "A java.io.Writer object representing standard output for print operations.\n\n  Defaults to System/out, wrapped in an OutputStreamWriter"}))
(when-let [v (resolve (quote clojure.core/*print-dup*))] (alter-meta! v fill {:doc "When set to logical true, objects will be printed in a way that preserves\n  their type when read in later.\n\n  Defaults to false."}))
(when-let [v (resolve (quote clojure.core/*print-length*))] (alter-meta! v fill {:doc "*print-length* controls how many items of each collection the\n  printer will print. If it is bound to logical false, there is no\n  limit. Otherwise, it must be bound to an integer indicating the maximum\n  number of items of each collection to print. If a collection contains\n  more items, the printer will print items up to the limit followed by\n  '...' to represent the remaining items. The root binding is nil\n  indicating no limit."}))
(when-let [v (resolve (quote clojure.core/*print-level*))] (alter-meta! v fill {:doc "*print-level* controls how many levels deep the printer will\n  print nested objects. If it is bound to logical false, there is no\n  limit. Otherwise, it must be bound to an integer indicating the maximum\n  level to print. Each argument to print is at level 0; if an argument is a\n  collection, its items are at level 1; and so on. If an object is a\n  collection and is at a level greater than or equal to the value bound to\n  *print-level*, the printer prints '#' to represent it. The root binding\n  is nil indicating no limit."}))
(when-let [v (resolve (quote clojure.core/*print-meta*))] (alter-meta! v fill {:doc "If set to logical true, when printing an object, its metadata will also\n  be printed in a form that can be read back by the reader.\n\n  Defaults to false."}))
(when-let [v (resolve (quote clojure.core/*print-namespace-maps*))] (alter-meta! v fill {:doc "*print-namespace-maps* controls whether the printer will print\n  namespace map literal syntax. It defaults to false, but the REPL binds\n  to true."}))
(when-let [v (resolve (quote clojure.core/*print-readably*))] (alter-meta! v fill {:doc "When set to logical false, strings and characters will be printed with\n  non-alphanumeric characters converted to the appropriate escape sequences.\n\n  Defaults to true"}))
(when-let [v (resolve (quote clojure.core/*unchecked-math*))] (alter-meta! v fill {:doc "While bound to true, compilations of +, -, *, inc, dec and the\n  coercions will be done without overflow checks. While bound\n  to :warn-on-boxed, same behavior as true, and a warning is emitted\n  when compilation uses boxed math. Default: false."}))
(when-let [v (resolve (quote clojure.core/*warn-on-reflection*))] (alter-meta! v fill {:doc "When set to true, the compiler will emit warnings when reflection is\n  needed to resolve Java method calls or field accesses.\n\n  Defaults to false."}))
(when-let [v (resolve (quote clojure.core/+))] (alter-meta! v fill {:arglists (quote ([] [x] [x y] [x y & more])), :doc "Returns the sum of nums. (+) returns 0. Does not auto-promote\n  longs, will throw on overflow. See also: +'"}))
(when-let [v (resolve (quote clojure.core/+'))] (alter-meta! v fill {:arglists (quote ([] [x] [x y] [x y & more])), :doc "Returns the sum of nums. (+') returns 0. Supports arbitrary precision.\n  See also: +"}))
(when-let [v (resolve (quote clojure.core/-))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "If no ys are supplied, returns the negation of x, else subtracts\n  the ys from x and returns the result. Does not auto-promote\n  longs, will throw on overflow. See also: -'"}))
(when-let [v (resolve (quote clojure.core/-'))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "If no ys are supplied, returns the negation of x, else subtracts\n  the ys from x and returns the result. Supports arbitrary precision.\n  See also: -"}))
(when-let [v (resolve (quote clojure.core/->))] (alter-meta! v fill {:arglists (quote ([x & forms])), :doc "Threads the expr through the forms. Inserts x as the\n  second item in the first form, making a list of it if it is not a\n  list already. If there are more forms, inserts the first form as the\n  second item in second form, etc."}))
(when-let [v (resolve (quote clojure.core/->>))] (alter-meta! v fill {:arglists (quote ([x & forms])), :doc "Threads the expr through the forms. Inserts x as the\n  last item in the first form, making a list of it if it is not a\n  list already. If there are more forms, inserts the first form as the\n  last item in second form, etc."}))
(when-let [v (resolve (quote clojure.core/->Eduction))] (alter-meta! v fill {:arglists (quote ([xform coll])), :doc "Positional factory function for class clojure.core.Eduction."}))
(when-let [v (resolve (quote clojure.core/..))] (alter-meta! v fill {:arglists (quote ([x form] [x form & more])), :doc "form => fieldName-symbol or (instanceMethodName-symbol args*)\n\n  Expands into a member access (.) of the first member on the first\n  argument, followed by the next member on the result, etc. For\n  instance:\n\n  (.. System (getProperties) (get \"os.name\"))\n\n  expands to:\n\n  (. (. System (getProperties)) (get \"os.name\"))\n\n  but is easier to write, read, and understand."}))
(when-let [v (resolve (quote clojure.core//))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "If no denominators are supplied, returns 1/numerator,\n  else returns numerator divided by all of the denominators."}))
(when-let [v (resolve (quote clojure.core/<))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns non-nil if nums are in monotonically increasing order,\n  otherwise false."}))
(when-let [v (resolve (quote clojure.core/<=))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns non-nil if nums are in monotonically non-decreasing order,\n  otherwise false."}))
(when-let [v (resolve (quote clojure.core/=))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Equality. Returns true if x equals y, false if not. Same as\n  Java x.equals(y) except it also works for nil, and compares\n  numbers and collections in a type-independent manner.  Clojure's immutable data\n  structures define equals() (and thus =) as a value, not an identity,\n  comparison."}))
(when-let [v (resolve (quote clojure.core/==))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns non-nil if nums all have the equivalent\n  value (type-independent), otherwise false"}))
(when-let [v (resolve (quote clojure.core/>))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns non-nil if nums are in monotonically decreasing order,\n  otherwise false."}))
(when-let [v (resolve (quote clojure.core/>=))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns non-nil if nums are in monotonically non-increasing order,\n  otherwise false."}))
(when-let [v (resolve (quote clojure.core/NaN?))] (alter-meta! v fill {:arglists (quote ([num])), :doc "Returns true if num is NaN, else false"}))
(when-let [v (resolve (quote clojure.core/Throwable->map))] (alter-meta! v fill {:arglists (quote ([o])), :doc "Constructs a data representation for a Throwable with keys:\n    :cause - root cause message\n    :phase - error phase\n    :via - cause chain, with cause keys:\n             :type - exception class symbol\n             :message - exception message\n             :data - ex-data\n             :at - top stack element\n    :trace - root cause stack elements"}))
(when-let [v (resolve (quote clojure.core/abs))] (alter-meta! v fill {:arglists (quote ([a])), :doc "Returns the absolute value of a.\n  If a is Long/MIN_VALUE => Long/MIN_VALUE\n  If a is a double and zero => +0.0\n  If a is a double and ##Inf or ##-Inf => ##Inf\n  If a is a double and ##NaN => ##NaN"}))
(when-let [v (resolve (quote clojure.core/aclone))] (alter-meta! v fill {:arglists (quote ([array])), :doc "Returns a clone of the Java array. Works on arrays of known\n  types."}))
(when-let [v (resolve (quote clojure.core/add-tap))] (alter-meta! v fill {:arglists (quote ([f])), :doc "adds f, a fn of one argument, to the tap set. This function will be called with anything sent via tap>.\n  This function may (briefly) block (e.g. for streams), and will never impede calls to tap>,\n  but blocking indefinitely may cause tap values to be dropped.\n  Remember f in order to remove-tap"}))
(when-let [v (resolve (quote clojure.core/add-watch))] (alter-meta! v fill {:arglists (quote ([reference key fn])), :doc "Adds a watch function to an agent/atom/var/ref reference. The watch\n  fn must be a fn of 4 args: a key, the reference, its old-state, its\n  new-state. Whenever the reference's state might have been changed,\n  any registered watches will have their functions called. The watch fn\n  will be called synchronously, on the agent's thread if an agent,\n  before any pending sends if agent or ref. Note that an atom's or\n  ref's state may have changed again prior to the fn call, so use\n  old/new-state rather than derefing the reference. Note also that watch\n  fns may be called from multiple threads simultaneously. Var watchers\n  are triggered only by root binding changes, not thread-local\n  set!s. Keys must be unique per reference, and can be used to remove\n  the watch with remove-watch, but are otherwise considered opaque by\n  the watch mechanism."}))
(when-let [v (resolve (quote clojure.core/agent))] (alter-meta! v fill {:arglists (quote ([state & options])), :doc "Creates and returns an agent with an initial value of state and\n  zero or more options (in any order):\n\n  :meta metadata-map\n\n  :validator validate-fn\n\n  :error-handler handler-fn\n\n  :error-mode mode-keyword\n\n  If metadata-map is supplied, it will become the metadata on the\n  agent. validate-fn must be nil or a side-effect-free fn of one\n  argument, which will be passed the intended new state on any state\n  change. If the new state is unacceptable, the validate-fn should\n  return false or throw an exception.  handler-fn is called if an\n  action throws an exception or if validate-fn rejects a new state --\n  see set-error-handler! for details.  The mode-keyword may be either\n  :continue (the default if an error-handler is given) or :fail (the\n  default if no error-handler is given) -- see set-error-mode! for\n  details."}))
(when-let [v (resolve (quote clojure.core/agent-error))] (alter-meta! v fill {:arglists (quote ([a])), :doc "Returns the exception thrown during an asynchronous action of the\n  agent if the agent is failed.  Returns nil if the agent is not\n  failed."}))
(when-let [v (resolve (quote clojure.core/agent-errors))] (alter-meta! v fill {:arglists (quote ([a])), :doc "DEPRECATED: Use 'agent-error' instead.\n  Returns a sequence of the exceptions thrown during asynchronous\n  actions of the agent."}))
(when-let [v (resolve (quote clojure.core/aget))] (alter-meta! v fill {:arglists (quote ([array idx] [array idx & idxs])), :doc "Returns the value at the index/indices. Works on Java arrays of all\n  types."}))
(when-let [v (resolve (quote clojure.core/alength))] (alter-meta! v fill {:arglists (quote ([array])), :doc "Returns the length of the Java array. Works on arrays of all\n  types."}))
(when-let [v (resolve (quote clojure.core/alias))] (alter-meta! v fill {:arglists (quote ([alias namespace-sym])), :doc "Add an alias in the current namespace to another\n  namespace. Arguments are two symbols: the alias to be used, and\n  the symbolic name of the target namespace. Use :as in the ns macro in preference\n  to calling this directly."}))
(when-let [v (resolve (quote clojure.core/all-ns))] (alter-meta! v fill {:arglists (quote ([])), :doc "Returns a sequence of all namespaces."}))
(when-let [v (resolve (quote clojure.core/alter))] (alter-meta! v fill {:arglists (quote ([ref fun & args])), :doc "Must be called in a transaction. Sets the in-transaction-value of\n  ref to:\n\n  (apply fun in-transaction-value-of-ref args)\n\n  and returns the in-transaction-value of ref."}))
(when-let [v (resolve (quote clojure.core/alter-meta!))] (alter-meta! v fill {:arglists (quote ([iref f & args])), :doc "Atomically sets the metadata for a namespace/var/ref/agent/atom to be:\n\n  (apply f its-current-meta args)\n\n  f must be free of side-effects"}))
(when-let [v (resolve (quote clojure.core/alter-var-root))] (alter-meta! v fill {:arglists (quote ([v f & args])), :doc "Atomically alters the root binding of var v by applying f to its\n  current value plus any args"}))
(when-let [v (resolve (quote clojure.core/amap))] (alter-meta! v fill {:arglists (quote ([a idx ret expr])), :doc "Maps an expression across an array a, using an index named idx, and\n  return value named ret, initialized to a clone of a, then setting \n  each element of ret to the evaluation of expr, returning the new \n  array ret."}))
(when-let [v (resolve (quote clojure.core/ancestors))] (alter-meta! v fill {:arglists (quote ([tag] [h tag])), :doc "Returns the immediate and indirect parents of tag, either via a Java type\n  inheritance relationship or a relationship established via derive. h\n  must be a hierarchy obtained from make-hierarchy, if not supplied\n  defaults to the global hierarchy"}))
(when-let [v (resolve (quote clojure.core/and))] (alter-meta! v fill {:arglists (quote ([] [x] [x & next])), :doc "Evaluates exprs one at a time, from left to right. If a form\n  returns logical false (nil or false), and returns that value and\n  doesn't evaluate any of the other expressions, otherwise it returns\n  the value of the last expr. (and) returns true."}))
(when-let [v (resolve (quote clojure.core/any?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true given any argument."}))
(when-let [v (resolve (quote clojure.core/apply))] (alter-meta! v fill {:arglists (quote ([f args] [f x args] [f x y args] [f x y z args] [f a b c d & args])), :doc "Applies fn f to the argument list formed by prepending intervening arguments to args."}))
(when-let [v (resolve (quote clojure.core/areduce))] (alter-meta! v fill {:arglists (quote ([a idx ret init expr])), :doc "Reduces an expression across an array a, using an index named idx,\n  and return value named ret, initialized to init, setting ret to the \n  evaluation of expr at each step, returning ret."}))
(when-let [v (resolve (quote clojure.core/array-map))] (alter-meta! v fill {:arglists (quote ([] [& keyvals])), :doc "Constructs an array-map. If any keys are equal, they are handled as\n  if by repeated uses of assoc."}))
(when-let [v (resolve (quote clojure.core/as->))] (alter-meta! v fill {:arglists (quote ([expr name & forms])), :doc "Binds name to expr, evaluates the first form in the lexical context\n  of that binding, then binds name to that result, repeating for each\n  successive form, returning the result of the last form."}))
(when-let [v (resolve (quote clojure.core/aset))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on Java arrays of\n  reference types. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-boolean))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of boolean. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-byte))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of byte. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-char))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of char. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-double))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of double. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-float))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of float. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-int))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of int. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-long))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of long. Returns val."}))
(when-let [v (resolve (quote clojure.core/aset-short))] (alter-meta! v fill {:arglists (quote ([array idx val] [array idx idx2 & idxv])), :doc "Sets the value at the index/indices. Works on arrays of short. Returns val."}))
(when-let [v (resolve (quote clojure.core/assert))] (alter-meta! v fill {:arglists (quote ([x] [x message])), :doc "Evaluates expression x and throws an AssertionError with optional\n  message if x does not evaluate to logical true.\n\n  Assertion checks are omitted from compiled code if '*assert*' is\n  false."}))
(when-let [v (resolve (quote clojure.core/assoc))] (alter-meta! v fill {:arglists (quote ([map key val] [map key val & kvs])), :doc "assoc[iate]. When applied to a map, returns a new map of the\n    same (hashed/sorted) type, that contains the mapping of key(s) to\n    val(s). When applied to a vector, returns a new vector that\n    contains val at index. Note - index must be <= (count vector)."}))
(when-let [v (resolve (quote clojure.core/assoc!))] (alter-meta! v fill {:arglists (quote ([coll key val] [coll key val & kvs])), :doc "When applied to a transient map, adds mapping of key(s) to\n  val(s). When applied to a transient vector, sets the val at index.\n  Note - index must be <= (count vector). Returns coll."}))
(when-let [v (resolve (quote clojure.core/assoc-in))] (alter-meta! v fill {:arglists (quote ([m [k & ks] v])), :doc "Associates a value in a nested associative structure, where ks is a\n  sequence of keys and v is the new value and returns a new nested structure.\n  If any levels do not exist, hash-maps will be created."}))
(when-let [v (resolve (quote clojure.core/associative?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns true if coll implements Associative"}))
(when-let [v (resolve (quote clojure.core/atom))] (alter-meta! v fill {:arglists (quote ([x] [x & options])), :doc "Creates and returns an Atom with an initial value of x and zero or\n  more options (in any order):\n\n  :meta metadata-map\n\n  :validator validate-fn\n\n  If metadata-map is supplied, it will become the metadata on the\n  atom. validate-fn must be nil or a side-effect-free fn of one\n  argument, which will be passed the intended new state on any state\n  change. If the new state is unacceptable, the validate-fn should\n  return false or throw an exception."}))
(when-let [v (resolve (quote clojure.core/await))] (alter-meta! v fill {:arglists (quote ([& agents])), :doc "Blocks the current thread (indefinitely!) until all actions\n  dispatched thus far, from this thread or agent, to the agent(s) have\n  occurred.  Will block on failed agents.  Will never return if\n  a failed agent is restarted with :clear-actions true or shutdown-agents was called."}))
(when-let [v (resolve (quote clojure.core/await-for))] (alter-meta! v fill {:arglists (quote ([timeout-ms & agents])), :doc "Blocks the current thread until all actions dispatched thus\n  far (from this thread or agent) to the agents have occurred, or the\n  timeout (in milliseconds) has elapsed. Returns logical false if\n  returning due to timeout, logical true otherwise."}))
(when-let [v (resolve (quote clojure.core/bigdec))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to BigDecimal"}))
(when-let [v (resolve (quote clojure.core/bigint))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to BigInt"}))
(when-let [v (resolve (quote clojure.core/biginteger))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to BigInteger"}))
(when-let [v (resolve (quote clojure.core/bit-and))] (alter-meta! v fill {:arglists (quote ([x y] [x y & more])), :doc "Bitwise and"}))
(when-let [v (resolve (quote clojure.core/bit-and-not))] (alter-meta! v fill {:arglists (quote ([x y] [x y & more])), :doc "Bitwise and with complement"}))
(when-let [v (resolve (quote clojure.core/bit-clear))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Clear bit at index n"}))
(when-let [v (resolve (quote clojure.core/bit-flip))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Flip bit at index n"}))
(when-let [v (resolve (quote clojure.core/bit-not))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Bitwise complement"}))
(when-let [v (resolve (quote clojure.core/bit-or))] (alter-meta! v fill {:arglists (quote ([x y] [x y & more])), :doc "Bitwise or"}))
(when-let [v (resolve (quote clojure.core/bit-set))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Set bit at index n"}))
(when-let [v (resolve (quote clojure.core/bit-shift-left))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Bitwise shift left"}))
(when-let [v (resolve (quote clojure.core/bit-shift-right))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Bitwise shift right"}))
(when-let [v (resolve (quote clojure.core/bit-test))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Test bit at index n"}))
(when-let [v (resolve (quote clojure.core/bit-xor))] (alter-meta! v fill {:arglists (quote ([x y] [x y & more])), :doc "Bitwise exclusive or"}))
(when-let [v (resolve (quote clojure.core/boolean))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to boolean"}))
(when-let [v (resolve (quote clojure.core/boolean-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of booleans"}))
(when-let [v (resolve (quote clojure.core/boolean?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a Boolean"}))
(when-let [v (resolve (quote clojure.core/bound-fn))] (alter-meta! v fill {:arglists (quote ([& fntail])), :doc "Returns a function defined by the given fntail, which will install the\n  same bindings in effect as in the thread at the time bound-fn was called.\n  This may be used to define a helper function which runs on a different\n  thread, but needs the same bindings in place."}))
(when-let [v (resolve (quote clojure.core/bound-fn*))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Returns a function, which will install the same bindings in effect as in\n  the thread at the time bound-fn* was called and then call f with any given\n  arguments. This may be used to define a helper function which runs on a\n  different thread, but needs the same bindings in place."}))
(when-let [v (resolve (quote clojure.core/bound?))] (alter-meta! v fill {:arglists (quote ([& vars])), :doc "Returns true if all of the vars provided as arguments have any bound value, root or thread-local.\n   Implies that deref'ing the provided vars will succeed. Returns true if no vars are provided."}))
(when-let [v (resolve (quote clojure.core/bounded-count))] (alter-meta! v fill {:arglists (quote ([n coll])), :doc "If coll is counted? returns its count, else will count at most the first n\n  elements of coll using its seq"}))
(when-let [v (resolve (quote clojure.core/butlast))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Return a seq of all but the last item in coll, in linear time"}))
(when-let [v (resolve (quote clojure.core/byte))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to byte"}))
(when-let [v (resolve (quote clojure.core/byte-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of bytes"}))
(when-let [v (resolve (quote clojure.core/bytes?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a byte array"}))
(when-let [v (resolve (quote clojure.core/case))] (alter-meta! v fill {:arglists (quote ([e & clauses])), :doc "Takes an expression, and a set of clauses.\n\n  Each clause can take the form of either:\n\n  test-constant result-expr\n\n  (test-constant1 ... test-constantN)  result-expr\n\n  The test-constants are not evaluated. They must be compile-time\n  literals, and need not be quoted.  If the expression is equal to a\n  test-constant, the corresponding result-expr is returned. A single\n  default expression can follow the clauses, and its value will be\n  returned if no clause matches. If no default expression is provided\n  and no clause matches, an IllegalArgumentException is thrown.\n\n  Unlike cond and condp, case does a constant-time dispatch, the\n  clauses are not considered sequentially.  All manner of constant\n  expressions are acceptable in case, including numbers, strings,\n  symbols, keywords, and (Clojure) composites thereof. Note that since\n  lists are used to group multiple constants that map to the same\n  expression, a vector can be used to match a list if needed. The\n  test-constants need not be all of the same type."}))
(when-let [v (resolve (quote clojure.core/cat))] (alter-meta! v fill {:arglists (quote ([rf])), :doc "A transducer which concatenates the contents of each input, which must be a\n  collection, into the reduction."}))
(when-let [v (resolve (quote clojure.core/char))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to char"}))
(when-let [v (resolve (quote clojure.core/char-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of chars"}))
(when-let [v (resolve (quote clojure.core/char-escape-string))] (alter-meta! v fill {:doc "Returns escape string for char or nil if none"}))
(when-let [v (resolve (quote clojure.core/char-name-string))] (alter-meta! v fill {:doc "Returns name string for char or nil if none"}))
(when-let [v (resolve (quote clojure.core/char?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a Character"}))
(when-let [v (resolve (quote clojure.core/chunk-append))] (alter-meta! v fill {:arglists (quote ([b x]))}))
(when-let [v (resolve (quote clojure.core/chunk-buffer))] (alter-meta! v fill {:arglists (quote ([capacity]))}))
(when-let [v (resolve (quote clojure.core/chunk-cons))] (alter-meta! v fill {:arglists (quote ([chunk rest]))}))
(when-let [v (resolve (quote clojure.core/chunk-rest))] (alter-meta! v fill {:arglists (quote ([s]))}))
(when-let [v (resolve (quote clojure.core/chunked-seq?))] (alter-meta! v fill {:arglists (quote ([s]))}))
(when-let [v (resolve (quote clojure.core/class))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the Class of x"}))
(when-let [v (resolve (quote clojure.core/class?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is an instance of Class"}))
(when-let [v (resolve (quote clojure.core/clear-agent-errors))] (alter-meta! v fill {:arglists (quote ([a])), :doc "DEPRECATED: Use 'restart-agent' instead.\n  Clears any exceptions thrown during asynchronous actions of the\n  agent, allowing subsequent actions to occur."}))
(when-let [v (resolve (quote clojure.core/clojure-version))] (alter-meta! v fill {:arglists (quote ([])), :doc "Returns clojure version as a printable string."}))
(when-let [v (resolve (quote clojure.core/coll?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x implements IPersistentCollection"}))
(when-let [v (resolve (quote clojure.core/comment))] (alter-meta! v fill {:arglists (quote ([& body])), :doc "Ignores body, yields nil"}))
(when-let [v (resolve (quote clojure.core/commute))] (alter-meta! v fill {:arglists (quote ([ref fun & args])), :doc "Must be called in a transaction. Sets the in-transaction-value of\n  ref to:\n\n  (apply fun in-transaction-value-of-ref args)\n\n  and returns the in-transaction-value of ref.\n\n  At the commit point of the transaction, sets the value of ref to be:\n\n  (apply fun most-recently-committed-value-of-ref args)\n\n  Thus fun should be commutative, or, failing that, you must accept\n  last-one-in-wins behavior.  commute allows for more concurrency than\n  ref-set."}))
(when-let [v (resolve (quote clojure.core/comp))] (alter-meta! v fill {:arglists (quote ([] [f] [f g] [f g & fs])), :doc "Takes a set of functions and returns a fn that is the composition\n  of those fns.  The returned fn takes a variable number of args,\n  applies the rightmost of fns to the args, the next\n  fn (right-to-left) to the result, etc."}))
(when-let [v (resolve (quote clojure.core/comparator))] (alter-meta! v fill {:arglists (quote ([pred])), :doc "Returns an implementation of java.util.Comparator based upon pred."}))
(when-let [v (resolve (quote clojure.core/compare))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Comparator. Returns a negative number, zero, or a positive number\n  when x is logically 'less than', 'equal to', or 'greater than'\n  y. Same as Java x.compareTo(y) except it also works for nil, and\n  compares numbers and collections in a type-independent manner. x\n  must implement Comparable"}))
(when-let [v (resolve (quote clojure.core/compare-and-set!))] (alter-meta! v fill {:arglists (quote ([atom oldval newval])), :doc "Atomically sets the value of atom to newval if and only if the\n  current value of the atom is identical to oldval. Returns true if\n  set happened, else false"}))
(when-let [v (resolve (quote clojure.core/complement))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Takes a fn f and returns a fn that takes the same arguments as f,\n  has the same effects, if any, and returns the opposite truth value."}))
(when-let [v (resolve (quote clojure.core/completing))] (alter-meta! v fill {:arglists (quote ([f] [f cf])), :doc "Takes a reducing function f of 2 args and returns a fn suitable for\n  transduce by adding an arity-1 signature that calls cf (default -\n  identity) on the result argument."}))
(when-let [v (resolve (quote clojure.core/concat))] (alter-meta! v fill {:arglists (quote ([] [x] [x y] [x y & zs])), :doc "Returns a lazy seq representing the concatenation of the elements in the supplied colls."}))
(when-let [v (resolve (quote clojure.core/cond))] (alter-meta! v fill {:arglists (quote ([& clauses])), :doc "Takes a set of test/expr pairs. It evaluates each test one at a\n  time.  If a test returns logical true, cond evaluates and returns\n  the value of the corresponding expr and doesn't evaluate any of the\n  other tests or exprs. (cond) returns nil."}))
(when-let [v (resolve (quote clojure.core/cond->))] (alter-meta! v fill {:arglists (quote ([expr & clauses])), :doc "Takes an expression and a set of test/form pairs. Threads expr (via ->)\n  through each form for which the corresponding test\n  expression is true. Note that, unlike cond branching, cond-> threading does\n  not short circuit after the first true test expression."}))
(when-let [v (resolve (quote clojure.core/cond->>))] (alter-meta! v fill {:arglists (quote ([expr & clauses])), :doc "Takes an expression and a set of test/form pairs. Threads expr (via ->>)\n  through each form for which the corresponding test expression\n  is true.  Note that, unlike cond branching, cond->> threading does not short circuit\n  after the first true test expression."}))
(when-let [v (resolve (quote clojure.core/condp))] (alter-meta! v fill {:arglists (quote ([pred expr & clauses])), :doc "Takes a binary predicate, an expression, and a set of clauses.\n  Each clause can take the form of either:\n\n  test-expr result-expr\n\n  test-expr :>> result-fn\n\n  Note :>> is an ordinary keyword.\n\n  For each clause, (pred test-expr expr) is evaluated. If it returns\n  logical true, the clause is a match. If a binary clause matches, the\n  result-expr is returned, if a ternary clause matches, its result-fn,\n  which must be a unary function, is called with the result of the\n  predicate as its argument, the result of that call being the return\n  value of condp. A single default expression can follow the clauses,\n  and its value will be returned if no clause matches. If no default\n  expression is provided and no clause matches, an\n  IllegalArgumentException is thrown."}))
(when-let [v (resolve (quote clojure.core/conj))] (alter-meta! v fill {:arglists (quote ([] [coll] [coll x] [coll x & xs])), :doc "conj[oin]. Returns a new collection with the xs\n    'added'. (conj nil item) returns (item).\n    (conj coll) returns coll. (conj) returns [].\n    The 'addition' may happen at different 'places' depending\n    on the concrete type."}))
(when-let [v (resolve (quote clojure.core/conj!))] (alter-meta! v fill {:arglists (quote ([] [coll] [coll x])), :doc "Adds x to the transient collection, and return coll. The 'addition'\n  may happen at different 'places' depending on the concrete type."}))
(when-let [v (resolve (quote clojure.core/cons))] (alter-meta! v fill {:arglists (quote ([x seq])), :doc "Returns a new seq where x is the first element and seq is\n    the rest."}))
(when-let [v (resolve (quote clojure.core/constantly))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a function that takes any number of arguments and returns x."}))
(when-let [v (resolve (quote clojure.core/contains?))] (alter-meta! v fill {:arglists (quote ([coll key])), :doc "Returns true if key is present in the given collection, otherwise\n  returns false.  Note that for numerically indexed collections like\n  vectors and Java arrays, this tests if the numeric key is within the\n  range of indexes. 'contains?' operates constant or logarithmic time;\n  it will not perform a linear search for a value.  See also 'some'."}))
(when-let [v (resolve (quote clojure.core/count))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns the number of items in the collection. (count nil) returns\n  0.  Also works on strings, arrays, and Java Collections and Maps"}))
(when-let [v (resolve (quote clojure.core/counted?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns true if coll implements count in constant time"}))
(when-let [v (resolve (quote clojure.core/create-ns))] (alter-meta! v fill {:arglists (quote ([sym])), :doc "Create a new namespace named by the symbol if one doesn't already\n  exist, returns it or the already-existing namespace of the same\n  name."}))
(when-let [v (resolve (quote clojure.core/cycle))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a lazy (infinite!) sequence of repetitions of the items in coll."}))
(when-let [v (resolve (quote clojure.core/dec))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one less than num. Does not auto-promote\n  longs, will throw on overflow. See also: dec'"}))
(when-let [v (resolve (quote clojure.core/dec'))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one less than num. Supports arbitrary precision.\n  See also: dec"}))
(when-let [v (resolve (quote clojure.core/decimal?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is a BigDecimal"}))
(when-let [v (resolve (quote clojure.core/declare))] (alter-meta! v fill {:arglists (quote ([& names])), :doc "defs the supplied var names with no bindings, useful for making forward declarations."}))
(when-let [v (resolve (quote clojure.core/dedupe))] (alter-meta! v fill {:arglists (quote ([] [coll])), :doc "Returns a lazy sequence removing consecutive duplicates in coll.\n  Returns a transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/default-data-readers))] (alter-meta! v fill {:doc "Default map of data reader functions provided by Clojure. May be\n  overridden by binding *data-readers*."}))
(when-let [v (resolve (quote clojure.core/definline))] (alter-meta! v fill {:arglists (quote ([name & decl])), :doc "Experimental - like defmacro, except defines a named function whose\n  body is the expansion, calls to which may be expanded inline as if\n  it were a macro. Cannot be used with variadic (&) args."}))
(when-let [v (resolve (quote clojure.core/definterface))] (alter-meta! v fill {:arglists (quote ([name & sigs])), :doc "Creates a new Java interface with the given name and method sigs.\n  The method return types and parameter types may be specified with type hints,\n  defaulting to Object if omitted.\n\n  (definterface MyInterface\n    (^int method1 [x])\n    (^Bar method2 [^Baz b ^Quux q]))"}))
(when-let [v (resolve (quote clojure.core/defmacro))] (alter-meta! v fill {:arglists (quote ([name doc-string? attr-map? [params*] body] [name doc-string? attr-map? ([params*] body) + attr-map?])), :doc "Like defn, but the resulting function name is declared as a\n  macro and will be used as a macro by the compiler when it is\n  called."}))
(when-let [v (resolve (quote clojure.core/defmethod))] (alter-meta! v fill {:arglists (quote ([multifn dispatch-val & fn-tail])), :doc "Creates and installs a new method of multimethod associated with dispatch-value. "}))
(when-let [v (resolve (quote clojure.core/defmulti))] (alter-meta! v fill {:arglists (quote ([name docstring? attr-map? dispatch-fn & options])), :doc "Creates a new multimethod with the associated dispatch function.\n  The docstring and attr-map are optional.\n\n  Options are key-value pairs and may be one of:\n\n  :default\n\n  The default dispatch value, defaults to :default\n\n  :hierarchy\n\n  The value used for hierarchical dispatch (e.g. ::square is-a ::shape)\n\n  Hierarchies are type-like relationships that do not depend upon type\n  inheritance. By default Clojure's multimethods dispatch off of a\n  global hierarchy map.  However, a hierarchy relationship can be\n  created with the derive function used to augment the root ancestor\n  created with make-hierarchy.\n\n  Multimethods expect the value of the hierarchy option to be supplied as\n  a reference type e.g. a var (i.e. via the Var-quote dispatch macro #'\n  or the var special form)."}))
(when-let [v (resolve (quote clojure.core/defn))] (alter-meta! v fill {:arglists (quote ([name doc-string? attr-map? [params*] prepost-map? body] [name doc-string? attr-map? ([params*] prepost-map? body) + attr-map?])), :doc "Same as (def name (fn [params* ] exprs*)) or (def\n    name (fn ([params* ] exprs*)+)) with any doc-string or attrs added\n    to the var metadata. prepost-map defines a map with optional keys\n    :pre and :post that contain collections of pre or post conditions."}))
(when-let [v (resolve (quote clojure.core/defn-))] (alter-meta! v fill {:arglists (quote ([name & decls])), :doc "same as defn, yielding non-public def"}))
(when-let [v (resolve (quote clojure.core/defonce))] (alter-meta! v fill {:arglists (quote ([name expr])), :doc "defs name to have the root value of the expr iff the named var has no root value,\n  else expr is unevaluated"}))
(when-let [v (resolve (quote clojure.core/defprotocol))] (alter-meta! v fill {:arglists (quote ([name & opts+sigs])), :doc "A protocol is a named set of named methods and their signatures:\n  (defprotocol AProtocolName\n\n    ;optional doc string\n    \"A doc string for AProtocol abstraction\"\n\n   ;options\n   :extend-via-metadata true\n\n  ;method signatures\n    (bar [this a b] \"bar docs\")\n    (baz [this a] [this a b] [this a b c] \"baz docs\"))\n\n  No implementations are provided. Docs can be specified for the\n  protocol overall and for each method. The above yields a set of\n  polymorphic functions and a protocol object. All are\n  namespace-qualified by the ns enclosing the definition The resulting\n  functions dispatch on the type of their first argument, which is\n  required and corresponds to the implicit target object ('this' in \n  Java parlance). defprotocol is dynamic, has no special compile-time \n  effect, and defines no new types or classes. Implementations of \n  the protocol methods can be provided using extend.\n\n  When :extend-via-metadata is true, values can extend protocols by\n  adding metadata where keys are fully-qualified protocol function\n  symbols and values are function implementations. Protocol\n  implementations are checked first for direct definitions (defrecord,\n  deftype, reify), then metadata definitions, then external\n  extensions (extend, extend-type, extend-protocol)\n\n  defprotocol will automatically generate a corresponding interface,\n  with the same name as the protocol, i.e. given a protocol:\n  my.ns/Protocol, an interface: my.ns.Protocol. The interface will\n  have methods corresponding to the protocol functions, and the\n  protocol will automatically work with instances of the interface.\n\n  Note that you should not use this interface with deftype or\n  reify, as they support the protocol directly:\n\n  (defprotocol P \n    (foo [this]) \n    (bar-me [this] [this y]))\n\n  (deftype Foo [a b c] \n   P\n    (foo [this] a)\n    (bar-me [this] b)\n    (bar-me [this y] (+ c y)))\n  \n  (bar-me (Foo. 1 2 3) 42)\n  => 45\n\n  (foo \n    (let [x 42]\n      (reify P \n        (foo [this] 17)\n        (bar-me [this] x)\n        (bar-me [this y] x))))\n  => 17"}))
(when-let [v (resolve (quote clojure.core/defrecord))] (alter-meta! v fill {:arglists (quote ([name [& fields] & opts+specs])), :doc "(defrecord name [fields*]  options* specs*)\n\n  Options are expressed as sequential keywords and arguments (in any order).\n\n  Supported options:\n  :load-ns - if true, importing the record class will cause the\n             namespace in which the record was defined to be loaded.\n             Defaults to false.\n\n  Each spec consists of a protocol or interface name followed by zero\n  or more method bodies:\n\n  protocol-or-interface-or-Object\n  (methodName [args*] body)*\n\n  Dynamically generates compiled bytecode for class with the given\n  name, in a package with the same name as the current namespace, the\n  given fields, and, optionally, methods for protocols and/or\n  interfaces.\n\n  The class will have the (immutable) fields named by\n  fields, which can have type hints. Protocols/interfaces and methods\n  are optional. The only methods that can be supplied are those\n  declared in the protocols/interfaces.  Note that method bodies are\n  not closures, the local environment includes only the named fields,\n  and those fields can be accessed directly.\n\n  Method definitions take the form:\n\n  (methodname [args*] body)\n\n  The argument and return types can be hinted on the arg and\n  methodname symbols. If not supplied, they will be inferred, so type\n  hints should be reserved for disambiguation.\n\n  Methods should be supplied for all methods of the desired\n  protocol(s) and interface(s). You can also define overrides for\n  methods of Object. Note that a parameter must be supplied to\n  correspond to the target object ('this' in Java parlance). Thus\n  methods for interfaces will take one more argument than do the\n  interface declarations. Note also that recur calls to the method\n  head should *not* pass the target object, it will be supplied\n  automatically and can not be substituted.\n\n  In the method bodies, the (unqualified) name can be used to name the\n  class (for calls to new, instance? etc).\n\n  The class will have implementations of several (clojure.lang)\n  interfaces generated automatically: IObj (metadata support) and\n  IPersistentMap, and all of their superinterfaces.\n\n  In addition, defrecord will define type-and-value-based =,\n  and will defined Java .hashCode and .equals consistent with the\n  contract for java.util.Map.\n\n  When AOT compiling, generates compiled bytecode for a class with the\n  given name (a symbol), prepends the current ns as the package, and\n  writes the .class file to the *compile-path* directory.\n\n  Two constructors will be defined, one taking the designated fields\n  followed by a metadata map (nil for none) and an extension field\n  map (nil for none), and one taking only the fields (using nil for\n  meta and extension fields). Note that the field names __meta,\n  __extmap, __hash and __hasheq are currently reserved and should not\n  be used when defining your own records.\n\n  Given (defrecord TypeName ...), two factory functions will be\n  defined: ->TypeName, taking positional parameters for the fields,\n  and map->TypeName, taking a map of keywords to field values."}))
(when-let [v (resolve (quote clojure.core/deftype))] (alter-meta! v fill {:arglists (quote ([name [& fields] & opts+specs])), :doc "(deftype name [fields*]  options* specs*)\n\n  Options are expressed as sequential keywords and arguments (in any order).\n\n  Supported options:\n  :load-ns - if true, importing the type class will cause the\n             namespace in which the type was defined to be loaded.\n             Defaults to false.\n\n  Each spec consists of a protocol or interface name followed by zero\n  or more method bodies:\n\n  protocol-or-interface-or-Object\n  (methodName [args*] body)*\n\n  Dynamically generates compiled bytecode for class with the given\n  name, in a package with the same name as the current namespace, the\n  given fields, and, optionally, methods for protocols and/or\n  interfaces. \n\n  The class will have the (by default, immutable) fields named by\n  fields, which can have type hints. Protocols/interfaces and methods\n  are optional. The only methods that can be supplied are those\n  declared in the protocols/interfaces.  Note that method bodies are\n  not closures, the local environment includes only the named fields,\n  and those fields can be accessed directly. Fields can be qualified\n  with the metadata :volatile-mutable true or :unsynchronized-mutable\n  true, at which point (set! afield aval) will be supported in method\n  bodies. Note well that mutable fields are extremely difficult to use\n  correctly, and are present only to facilitate the building of higher\n  level constructs, such as Clojure's reference types, in Clojure\n  itself. They are for experts only - if the semantics and\n  implications of :volatile-mutable or :unsynchronized-mutable are not\n  immediately apparent to you, you should not be using them.\n\n  Method definitions take the form:\n\n  (methodname [args*] body)\n\n  The argument and return types can be hinted on the arg and\n  methodname symbols. If not supplied, they will be inferred, so type\n  hints should be reserved for disambiguation.\n\n  Methods should be supplied for all methods of the desired\n  protocol(s) and interface(s). You can also define overrides for\n  methods of Object. Note that a parameter must be supplied to\n  correspond to the target object ('this' in Java parlance). Thus\n  methods for interfaces will take one more argument than do the\n  interface declarations. Note also that recur calls to the method\n  head should *not* pass the target object, it will be supplied\n  automatically and can not be substituted.\n\n  In the method bodies, the (unqualified) name can be used to name the\n  class (for calls to new, instance? etc).\n\n  When AOT compiling, generates compiled bytecode for a class with the\n  given name (a symbol), prepends the current ns as the package, and\n  writes the .class file to the *compile-path* directory.\n\n  One constructor will be defined, taking the designated fields.  Note\n  that the field names __meta, __extmap, __hash and __hasheq are currently\n  reserved and should not be used when defining your own types.\n\n  Given (deftype TypeName ...), a factory function called ->TypeName\n  will be defined, taking positional parameters for the fields"}))
(when-let [v (resolve (quote clojure.core/delay))] (alter-meta! v fill {:arglists (quote ([& body])), :doc "Takes a body of expressions and yields a Delay object that will\n  invoke the body only the first time it is forced (with force or deref/@), and\n  will cache the result and return it on all subsequent force\n  calls. See also - realized?"}))
(when-let [v (resolve (quote clojure.core/delay?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "returns true if x is a Delay created with delay"}))
(when-let [v (resolve (quote clojure.core/deliver))] (alter-meta! v fill {:arglists (quote ([promise val])), :doc "Delivers the supplied value to the promise, releasing any pending\n  derefs. A subsequent call to deliver on a promise will have no effect."}))
(when-let [v (resolve (quote clojure.core/denominator))] (alter-meta! v fill {:arglists (quote ([r])), :doc "Returns the denominator part of a Ratio."}))
(when-let [v (resolve (quote clojure.core/deref))] (alter-meta! v fill {:arglists (quote ([ref] [ref timeout-ms timeout-val])), :doc "Also reader macro: @ref/@agent/@var/@atom/@delay/@future/@promise. Within a transaction,\n  returns the in-transaction-value of ref, else returns the\n  most-recently-committed value of ref. When applied to a var, agent\n  or atom, returns its current state. When applied to a delay, forces\n  it if not already forced. When applied to a future, will block if\n  computation not complete. When applied to a promise, will block\n  until a value is delivered.  The variant taking a timeout can be\n  used for blocking references (futures and promises), and will return\n  timeout-val if the timeout (in milliseconds) is reached before a\n  value is available. See also - realized?."}))
(when-let [v (resolve (quote clojure.core/derive))] (alter-meta! v fill {:arglists (quote ([tag parent] [h tag parent])), :doc "Establishes a parent/child relationship between parent and\n  tag. Parent must be a namespace-qualified symbol or keyword and\n  child can be either a namespace-qualified symbol or keyword or a\n  class. h must be a hierarchy obtained from make-hierarchy, if not\n  supplied defaults to, and modifies, the global hierarchy."}))
(when-let [v (resolve (quote clojure.core/descendants))] (alter-meta! v fill {:arglists (quote ([tag] [h tag])), :doc "Returns the immediate and indirect children of tag, through a\n  relationship established via derive. h must be a hierarchy obtained\n  from make-hierarchy, if not supplied defaults to the global\n  hierarchy. Note: does not work on Java type inheritance\n  relationships."}))
(when-let [v (resolve (quote clojure.core/destructure))] (alter-meta! v fill {:arglists (quote ([bindings]))}))
(when-let [v (resolve (quote clojure.core/disj))] (alter-meta! v fill {:arglists (quote ([set] [set key] [set key & ks])), :doc "disj[oin]. Returns a new set of the same (hashed/sorted) type, that\n  does not contain key(s)."}))
(when-let [v (resolve (quote clojure.core/disj!))] (alter-meta! v fill {:arglists (quote ([set] [set key] [set key & ks])), :doc "disj[oin]. Returns a transient set of the same (hashed/sorted) type, that\n  does not contain key(s)."}))
(when-let [v (resolve (quote clojure.core/dissoc))] (alter-meta! v fill {:arglists (quote ([map] [map key] [map key & ks])), :doc "dissoc[iate]. Returns a new map of the same (hashed/sorted) type,\n  that does not contain a mapping for key(s)."}))
(when-let [v (resolve (quote clojure.core/dissoc!))] (alter-meta! v fill {:arglists (quote ([map key] [map key & ks])), :doc "Returns a transient map that doesn't contain a mapping for key(s)."}))
(when-let [v (resolve (quote clojure.core/distinct))] (alter-meta! v fill {:arglists (quote ([] [coll])), :doc "Returns a lazy sequence of the elements of coll with duplicates removed.\n  Returns a stateful transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/distinct?))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns true if no two of the arguments are ="}))
(when-let [v (resolve (quote clojure.core/doall))] (alter-meta! v fill {:arglists (quote ([coll] [n coll])), :doc "When lazy sequences are produced via functions that have side\n  effects, any effects other than those needed to produce the first\n  element in the seq do not occur until the seq is consumed. doall can\n  be used to force any effects. Walks through the successive nexts of\n  the seq, retains the head and returns it, thus causing the entire\n  seq to reside in memory at one time."}))
(when-let [v (resolve (quote clojure.core/dorun))] (alter-meta! v fill {:arglists (quote ([coll] [n coll])), :doc "When lazy sequences are produced via functions that have side\n  effects, any effects other than those needed to produce the first\n  element in the seq do not occur until the seq is consumed. dorun can\n  be used to force any effects. Walks through the successive nexts of\n  the seq, does not retain the head and returns nil."}))
(when-let [v (resolve (quote clojure.core/doseq))] (alter-meta! v fill {:arglists (quote ([seq-exprs & body])), :doc "Repeatedly executes body (presumably for side-effects) with\n  bindings and filtering as provided by \"for\".  Does not retain\n  the head of the sequence. Returns nil."}))
(when-let [v (resolve (quote clojure.core/dosync))] (alter-meta! v fill {:arglists (quote ([& exprs])), :doc "Runs the exprs (in an implicit do) in a transaction that encompasses\n  exprs and any nested calls.  Starts a transaction if none is already\n  running on this thread. Any uncaught exception will abort the\n  transaction and flow out of dosync. The exprs may be run more than\n  once, but any effects on Refs will be atomic."}))
(when-let [v (resolve (quote clojure.core/dotimes))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "bindings => name n\n\n  Repeatedly executes body (presumably for side-effects) with name\n  bound to integers from 0 through n-1."}))
(when-let [v (resolve (quote clojure.core/doto))] (alter-meta! v fill {:arglists (quote ([x & forms])), :doc "Evaluates x then calls all of the methods and functions with the\n  value of x supplied at the front of the given arguments.  The forms\n  are evaluated in order.  Returns x.\n\n  (doto (new java.util.HashMap) (.put \"a\" 1) (.put \"b\" 2))"}))
(when-let [v (resolve (quote clojure.core/double))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to double"}))
(when-let [v (resolve (quote clojure.core/double-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of doubles"}))
(when-let [v (resolve (quote clojure.core/double?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a Double"}))
(when-let [v (resolve (quote clojure.core/drop))] (alter-meta! v fill {:arglists (quote ([n] [n coll])), :doc "Returns a laziness-preserving sequence of all but the first n items in coll.\n  Returns a stateful transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/drop-last))] (alter-meta! v fill {:arglists (quote ([coll] [n coll])), :doc "Return a lazy sequence of all but the last n (default 1) items in coll"}))
(when-let [v (resolve (quote clojure.core/drop-while))] (alter-meta! v fill {:arglists (quote ([pred] [pred coll])), :doc "Returns a lazy sequence of the items in coll starting from the\n  first item for which (pred item) returns logical false.  Returns a\n  stateful transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/eduction))] (alter-meta! v fill {:arglists (quote ([xform* coll])), :doc "Returns a reducible/iterable application of the transducers\n  to the items in coll. Transducers are applied in order as if\n  combined with comp. Note that these applications will be\n  performed every time reduce/iterator is called."}))
(when-let [v (resolve (quote clojure.core/empty))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns an empty collection of the same category as coll, or nil"}))
(when-let [v (resolve (quote clojure.core/empty?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns true if coll has no items. To check the emptiness of a seq,\n  please use the idiom (seq x) rather than (not (empty? x))"}))
(when-let [v (resolve (quote clojure.core/ensure))] (alter-meta! v fill {:arglists (quote ([ref])), :doc "Must be called in a transaction. Protects the ref from modification\n  by other transactions.  Returns the in-transaction-value of\n  ref. Allows for more concurrency than (ref-set ref @ref)"}))
(when-let [v (resolve (quote clojure.core/ensure-reduced))] (alter-meta! v fill {:arglists (quote ([x])), :doc "If x is already reduced?, returns it, else returns (reduced x)"}))
(when-let [v (resolve (quote clojure.core/enumeration-seq))] (alter-meta! v fill {:arglists (quote ([e])), :doc "Returns a seq on a java.util.Enumeration"}))
(when-let [v (resolve (quote clojure.core/error-handler))] (alter-meta! v fill {:arglists (quote ([a])), :doc "Returns the error-handler of agent a, or nil if there is none.\n  See set-error-handler!"}))
(when-let [v (resolve (quote clojure.core/error-mode))] (alter-meta! v fill {:arglists (quote ([a])), :doc "Returns the error-mode of agent a.  See set-error-mode!"}))
(when-let [v (resolve (quote clojure.core/eval))] (alter-meta! v fill {:arglists (quote ([form])), :doc "Evaluates the form data structure (not text!) and returns the result."}))
(when-let [v (resolve (quote clojure.core/even?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is even, throws an exception if n is not an integer"}))
(when-let [v (resolve (quote clojure.core/every-pred))] (alter-meta! v fill {:arglists (quote ([p] [p1 p2] [p1 p2 p3] [p1 p2 p3 & ps])), :doc "Takes a set of predicates and returns a function f that returns true if all of its\n  composing predicates return a logical true value against all of its arguments, else it returns\n  false. Note that f is short-circuiting in that it will stop execution on the first\n  argument that triggers a logical false result against the original predicates."}))
(when-let [v (resolve (quote clojure.core/every?))] (alter-meta! v fill {:arglists (quote ([pred coll])), :doc "Returns true if (pred x) is logical true for every x in coll, else\n  false."}))
(when-let [v (resolve (quote clojure.core/ex-cause))] (alter-meta! v fill {:arglists (quote ([ex])), :doc "Returns the cause of ex if ex is a Throwable.\n  Otherwise returns nil."}))
(when-let [v (resolve (quote clojure.core/ex-data))] (alter-meta! v fill {:arglists (quote ([ex])), :doc "Returns exception data (a map) if ex is an IExceptionInfo.\n   Otherwise returns nil."}))
(when-let [v (resolve (quote clojure.core/ex-info))] (alter-meta! v fill {:arglists (quote ([msg map] [msg map cause])), :doc "Create an instance of ExceptionInfo, a RuntimeException subclass\n   that carries a map of additional data."}))
(when-let [v (resolve (quote clojure.core/ex-message))] (alter-meta! v fill {:arglists (quote ([ex])), :doc "Returns the message attached to ex if ex is a Throwable.\n  Otherwise returns nil."}))
(when-let [v (resolve (quote clojure.core/extend))] (alter-meta! v fill {:arglists (quote ([atype & proto+mmaps])), :doc "Implementations of protocol methods can be provided using the extend construct:\n\n  (extend AType\n    AProtocol\n     {:foo an-existing-fn\n      :bar (fn [a b] ...)\n      :baz (fn ([a]...) ([a b] ...)...)}\n    BProtocol \n      {...} \n    ...)\n \n  extend takes a type/class (or interface, see below), and one or more\n  protocol + method map pairs. It will extend the polymorphism of the\n  protocol's methods to call the supplied methods when an AType is\n  provided as the first argument. \n\n  Method maps are maps of the keyword-ized method names to ordinary\n  fns. This facilitates easy reuse of existing fns and fn maps, for\n  code reuse/mixins without derivation or composition. You can extend\n  an interface to a protocol. This is primarily to facilitate interop\n  with the host (e.g. Java) but opens the door to incidental multiple\n  inheritance of implementation since a class can inherit from more\n  than one interface, both of which extend the protocol. It is TBD how\n  to specify which impl to use. You can extend a protocol on nil.\n\n  If you are supplying the definitions explicitly (i.e. not reusing\n  exsting functions or mixin maps), you may find it more convenient to\n  use the extend-type or extend-protocol macros.\n\n  Note that multiple independent extend clauses can exist for the same\n  type, not all protocols need be defined in a single extend call.\n\n  See also:\n  extends?, satisfies?, extenders"}))
(when-let [v (resolve (quote clojure.core/extend-protocol))] (alter-meta! v fill {:arglists (quote ([p & specs])), :doc "Useful when you want to provide several implementations of the same\n  protocol all at once. Takes a single protocol and the implementation\n  of that protocol for one or more types. Expands into calls to\n  extend-type:\n\n  (extend-protocol Protocol\n    AType\n      (foo [x] ...)\n      (bar [x y] ...)\n    BType\n      (foo [x] ...)\n      (bar [x y] ...)\n    AClass\n      (foo [x] ...)\n      (bar [x y] ...)\n    nil\n      (foo [x] ...)\n      (bar [x y] ...))\n\n  expands into:\n\n  (do\n   (clojure.core/extend-type AType Protocol \n     (foo [x] ...) \n     (bar [x y] ...))\n   (clojure.core/extend-type BType Protocol \n     (foo [x] ...) \n     (bar [x y] ...))\n   (clojure.core/extend-type AClass Protocol \n     (foo [x] ...) \n     (bar [x y] ...))\n   (clojure.core/extend-type nil Protocol \n     (foo [x] ...) \n     (bar [x y] ...)))"}))
(when-let [v (resolve (quote clojure.core/extend-type))] (alter-meta! v fill {:arglists (quote ([t & specs])), :doc "A macro that expands into an extend call. Useful when you are\n  supplying the definitions explicitly inline, extend-type\n  automatically creates the maps required by extend.  Propagates the\n  class as a type hint on the first argument of all fns.\n\n  (extend-type MyType \n    Countable\n      (cnt [c] ...)\n    Foo\n      (bar [x y] ...)\n      (baz ([x] ...) ([x y & zs] ...)))\n\n  expands into:\n\n  (extend MyType\n   Countable\n     {:cnt (fn [c] ...)}\n   Foo\n     {:baz (fn ([x] ...) ([x y & zs] ...))\n      :bar (fn [x y] ...)})"}))
(when-let [v (resolve (quote clojure.core/extends?))] (alter-meta! v fill {:arglists (quote ([protocol atype])), :doc "Returns true if atype extends protocol"}))
(when-let [v (resolve (quote clojure.core/false?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is the value false, false otherwise."}))
(when-let [v (resolve (quote clojure.core/ffirst))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Same as (first (first x))"}))
(when-let [v (resolve (quote clojure.core/filter))] (alter-meta! v fill {:arglists (quote ([pred] [pred coll])), :doc "Returns a lazy sequence of the items in coll for which\n  (pred item) returns logical true. pred must be free of side-effects.\n  Returns a transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/filterv))] (alter-meta! v fill {:arglists (quote ([pred coll])), :doc "Returns a vector of the items in coll for which\n  (pred item) returns logical true. pred must be free of side-effects."}))
(when-let [v (resolve (quote clojure.core/find))] (alter-meta! v fill {:arglists (quote ([map key])), :doc "Returns the map entry for key, or nil if key not present."}))
(when-let [v (resolve (quote clojure.core/find-keyword))] (alter-meta! v fill {:arglists (quote ([name] [ns name])), :doc "Returns a Keyword with the given namespace and name if one already\n  exists.  This function will not intern a new keyword. If the keyword\n  has not already been interned, it will return nil.  Do not use :\n  in the keyword strings, it will be added automatically."}))
(when-let [v (resolve (quote clojure.core/find-ns))] (alter-meta! v fill {:arglists (quote ([sym])), :doc "Returns the namespace named by the symbol or nil if it doesn't exist."}))
(when-let [v (resolve (quote clojure.core/find-var))] (alter-meta! v fill {:arglists (quote ([sym])), :doc "Returns the global var named by the namespace-qualified symbol, or\n  nil if no var with that name."}))
(when-let [v (resolve (quote clojure.core/first))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns the first item in the collection. Calls seq on its\n    argument. If coll is nil, returns nil."}))
(when-let [v (resolve (quote clojure.core/flatten))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Takes any nested combination of sequential things (lists, vectors,\n  etc.) and returns their contents as a single, flat lazy sequence.\n  (flatten nil) returns an empty sequence."}))
(when-let [v (resolve (quote clojure.core/float))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to float"}))
(when-let [v (resolve (quote clojure.core/float-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of floats"}))
(when-let [v (resolve (quote clojure.core/float?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is a floating point number"}))
(when-let [v (resolve (quote clojure.core/flush))] (alter-meta! v fill {:arglists (quote ([])), :doc "Flushes the output stream that is the current value of\n  *out*"}))
(when-let [v (resolve (quote clojure.core/fn))] (alter-meta! v fill {:arglists (quote ([& sigs])), :doc "params => positional-params*, or positional-params* & rest-param\n  positional-param => binding-form\n  rest-param => binding-form\n  binding-form => name, or destructuring-form\n\n  Defines a function.\n\n  See https://clojure.org/reference/special_forms#fn for more information"}))
(when-let [v (resolve (quote clojure.core/fn?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x implements Fn, i.e. is an object created via fn."}))
(when-let [v (resolve (quote clojure.core/fnext))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Same as (first (next x))"}))
(when-let [v (resolve (quote clojure.core/fnil))] (alter-meta! v fill {:arglists (quote ([f x] [f x y] [f x y z])), :doc "Takes a function f, and returns a function that calls f, replacing\n  a nil first argument to f with the supplied value x. Higher arity\n  versions can replace arguments in the second and third\n  positions (y, z). Note that the function f can take any number of\n  arguments, not just the one(s) being nil-patched."}))
(when-let [v (resolve (quote clojure.core/for))] (alter-meta! v fill {:arglists (quote ([seq-exprs body-expr])), :doc "List comprehension. Takes a vector of one or more\n   binding-form/collection-expr pairs, each followed by zero or more\n   modifiers, and yields a lazy sequence of evaluations of expr.\n   Collections are iterated in a nested fashion, rightmost fastest,\n   and nested coll-exprs can refer to bindings created in prior\n   binding-forms.  Supported modifiers are: :let [binding-form expr ...],\n   :while test, :when test.\n\n  (take 100 (for [x (range 100000000) y (range 1000000) :while (< y x)] [x y]))"}))
(when-let [v (resolve (quote clojure.core/force))] (alter-meta! v fill {:arglists (quote ([x])), :doc "If x is a Delay, returns the (possibly cached) value of its expression, else returns x"}))
(when-let [v (resolve (quote clojure.core/format))] (alter-meta! v fill {:arglists (quote ([fmt & args])), :doc "Formats a string using java.lang.String.format, see java.util.Formatter for format\n  string syntax"}))
(when-let [v (resolve (quote clojure.core/frequencies))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a map from distinct items in coll to the number of times\n  they appear."}))
(when-let [v (resolve (quote clojure.core/future))] (alter-meta! v fill {:arglists (quote ([& body])), :doc "Takes a body of expressions and yields a future object that will\n  invoke the body in another thread, and will cache the result and\n  return it on all subsequent calls to deref/@. If the computation has\n  not yet finished, calls to deref/@ will block, unless the variant of\n  deref with timeout is used. See also - realized?."}))
(when-let [v (resolve (quote clojure.core/future-call))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Takes a function of no args and yields a future object that will\n  invoke the function in another thread, and will cache the result and\n  return it on all subsequent calls to deref/@. If the computation has\n  not yet finished, calls to deref/@ will block, unless the variant\n  of deref with timeout is used. See also - realized?."}))
(when-let [v (resolve (quote clojure.core/future-cancel))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Cancels the future, if possible."}))
(when-let [v (resolve (quote clojure.core/future-cancelled?))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Returns true if future f is cancelled"}))
(when-let [v (resolve (quote clojure.core/future-done?))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Returns true if future f is done"}))
(when-let [v (resolve (quote clojure.core/future?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is a future"}))
(when-let [v (resolve (quote clojure.core/gensym))] (alter-meta! v fill {:arglists (quote ([] [prefix-string])), :doc "Returns a new symbol with a unique name. If a prefix string is\n  supplied, the name is prefix# where # is some unique number. If\n  prefix is not supplied, the prefix is 'G__'."}))
(when-let [v (resolve (quote clojure.core/get))] (alter-meta! v fill {:arglists (quote ([map key] [map key not-found])), :doc "Returns the value mapped to key, not-found or nil if key not present\n  in associative collection, set, string, array, or ILookup instance."}))
(when-let [v (resolve (quote clojure.core/get-in))] (alter-meta! v fill {:arglists (quote ([m ks] [m ks not-found])), :doc "Returns the value in a nested associative structure,\n  where ks is a sequence of keys. Returns nil if the key\n  is not present, or the not-found value if supplied."}))
(when-let [v (resolve (quote clojure.core/get-method))] (alter-meta! v fill {:arglists (quote ([multifn dispatch-val])), :doc "Given a multimethod and a dispatch value, returns the dispatch fn\n  that would apply to that value, or nil if none apply and no default"}))
(when-let [v (resolve (quote clojure.core/get-thread-bindings))] (alter-meta! v fill {:arglists (quote ([])), :doc "Get a map with the Var/value pairs which is currently in effect for the\n  current thread."}))
(when-let [v (resolve (quote clojure.core/get-validator))] (alter-meta! v fill {:arglists (quote ([iref])), :doc "Gets the validator-fn for a var/ref/agent/atom."}))
(when-let [v (resolve (quote clojure.core/group-by))] (alter-meta! v fill {:arglists (quote ([f coll])), :doc "Returns a map of the elements of coll keyed by the result of\n  f on each element. The value at each key will be a vector of the\n  corresponding elements, in the order they appeared in coll."}))
(when-let [v (resolve (quote clojure.core/halt-when))] (alter-meta! v fill {:arglists (quote ([pred] [pred retf])), :doc "Returns a transducer that ends transduction when pred returns true\n  for an input. When retf is supplied it must be a fn of 2 arguments -\n  it will be passed the (completed) result so far and the input that\n  triggered the predicate, and its return value (if it does not throw\n  an exception) will be the return value of the transducer. If retf\n  is not supplied, the input that triggered the predicate will be\n  returned. If the predicate never returns true the transduction is\n  unaffected."}))
(when-let [v (resolve (quote clojure.core/hash))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the hash code of its argument. Note this is the hash code\n  consistent with =, and thus is different than .hashCode for Integer,\n  Short, Byte and Clojure collections."}))
(when-let [v (resolve (quote clojure.core/hash-map))] (alter-meta! v fill {:arglists (quote ([] [& keyvals])), :doc "keyval => key val\n  Returns a new hash map with supplied mappings.  If any keys are\n  equal, they are handled as if by repeated uses of assoc."}))
(when-let [v (resolve (quote clojure.core/hash-ordered-coll))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns the hash code, consistent with =, for an external ordered\n   collection implementing Iterable.\n   See http://clojure.org/data_structures#hash for full algorithms."}))
(when-let [v (resolve (quote clojure.core/hash-set))] (alter-meta! v fill {:arglists (quote ([] [& keys])), :doc "Returns a new hash set with supplied keys.  Any equal keys are\n  handled as if by repeated uses of conj."}))
(when-let [v (resolve (quote clojure.core/hash-unordered-coll))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns the hash code, consistent with =, for an external unordered\n   collection implementing Iterable. For maps, the iterator should\n   return map entries whose hash is computed as\n     (hash-ordered-coll [k v]).\n   See http://clojure.org/data_structures#hash for full algorithms."}))
(when-let [v (resolve (quote clojure.core/ident?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a symbol or keyword"}))
(when-let [v (resolve (quote clojure.core/identical?))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Tests if 2 arguments are the same object"}))
(when-let [v (resolve (quote clojure.core/identity))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns its argument."}))
(when-let [v (resolve (quote clojure.core/if-let))] (alter-meta! v fill {:arglists (quote ([bindings then] [bindings then else & oldform])), :doc "bindings => binding-form test\n\n  If test is true, evaluates then with binding-form bound to the value of \n  test, if not, yields else"}))
(when-let [v (resolve (quote clojure.core/if-not))] (alter-meta! v fill {:arglists (quote ([test then] [test then else])), :doc "Evaluates test. If logical false, evaluates and returns then expr, \n  otherwise else expr, if supplied, else nil."}))
(when-let [v (resolve (quote clojure.core/if-some))] (alter-meta! v fill {:arglists (quote ([bindings then] [bindings then else & oldform])), :doc "bindings => binding-form test\n\n   If test is not nil, evaluates then with binding-form bound to the\n   value of test, if not, yields else"}))
(when-let [v (resolve (quote clojure.core/ifn?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x implements IFn. Note that many data structures\n  (e.g. sets and maps) implement IFn"}))
(when-let [v (resolve (quote clojure.core/import))] (alter-meta! v fill {:arglists (quote ([& import-symbols-or-lists])), :doc "import-list => (package-symbol class-name-symbols*)\n\n  For each name in class-name-symbols, adds a mapping from name to the\n  class named by package.name to the current namespace. Use :import in the ns\n  macro in preference to calling this directly."}))
(when-let [v (resolve (quote clojure.core/in-ns))] (alter-meta! v fill {:arglists (quote ([name])), :doc "Sets *ns* to the namespace named by the symbol, creating it if needed."}))
(when-let [v (resolve (quote clojure.core/inc))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one greater than num. Does not auto-promote\n  longs, will throw on overflow. See also: inc'"}))
(when-let [v (resolve (quote clojure.core/inc'))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one greater than num. Supports arbitrary precision.\n  See also: inc"}))
(when-let [v (resolve (quote clojure.core/indexed?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Return true if coll implements Indexed, indicating efficient lookup by index"}))
(when-let [v (resolve (quote clojure.core/infinite?))] (alter-meta! v fill {:arglists (quote ([num])), :doc "Returns true if num is negative or positive infinity, else false"}))
(when-let [v (resolve (quote clojure.core/inst-ms))] (alter-meta! v fill {:arglists (quote ([inst])), :doc "Return the number of milliseconds since January 1, 1970, 00:00:00 GMT"}))
(when-let [v (resolve (quote clojure.core/inst?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x satisfies Inst"}))
(when-let [v (resolve (quote clojure.core/instance?))] (alter-meta! v fill {:arglists (quote ([c x])), :doc "Evaluates x and tests if it is an instance of the class\n    c. Returns true or false"}))
(when-let [v (resolve (quote clojure.core/int))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to int"}))
(when-let [v (resolve (quote clojure.core/int-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of ints"}))
(when-let [v (resolve (quote clojure.core/int?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a fixed precision integer"}))
(when-let [v (resolve (quote clojure.core/integer?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is an integer"}))
(when-let [v (resolve (quote clojure.core/interleave))] (alter-meta! v fill {:arglists (quote ([] [c1] [c1 c2] [c1 c2 & colls])), :doc "Returns a lazy seq of the first item in each coll, then the second etc."}))
(when-let [v (resolve (quote clojure.core/intern))] (alter-meta! v fill {:arglists (quote ([ns name] [ns name val])), :doc "Finds or creates a var named by the symbol name in the namespace\n  ns (which can be a symbol or a namespace), setting its root binding\n  to val if supplied. The namespace must exist. The var will adopt any\n  metadata from the name symbol.  Returns the var."}))
(when-let [v (resolve (quote clojure.core/interpose))] (alter-meta! v fill {:arglists (quote ([sep] [sep coll])), :doc "Returns a lazy seq of the elements of coll separated by sep.\n  Returns a stateful transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/into))] (alter-meta! v fill {:arglists (quote ([] [to] [to from] [to xform from])), :doc "Returns a new coll consisting of to with all of the items of\n  from conjoined. A transducer may be supplied.\n  (into x) returns x. (into) returns []."}))
(when-let [v (resolve (quote clojure.core/into-array))] (alter-meta! v fill {:arglists (quote ([aseq] [type aseq])), :doc "Returns an array with components set to the values in aseq. The array's\n  component type is type if provided, or the type of the first value in\n  aseq if present, or Object. All values in aseq must be compatible with\n  the component type. Class objects for the primitive types can be obtained\n  using, e.g., Integer/TYPE."}))
(when-let [v (resolve (quote clojure.core/io!))] (alter-meta! v fill {:arglists (quote ([& body])), :doc "If an io! block occurs in a transaction, throws an\n  IllegalStateException, else runs body in an implicit do. If the\n  first expression in body is a literal string, will use that as the\n  exception message."}))
(when-let [v (resolve (quote clojure.core/isa?))] (alter-meta! v fill {:arglists (quote ([child parent] [h child parent])), :doc "Returns true if (= child parent), or child is directly or indirectly derived from\n  parent, either via a Java type inheritance relationship or a\n  relationship established via derive. h must be a hierarchy obtained\n  from make-hierarchy, if not supplied defaults to the global\n  hierarchy"}))
(when-let [v (resolve (quote clojure.core/iterate))] (alter-meta! v fill {:arglists (quote ([f x])), :doc "Returns a lazy (infinite!) sequence of x, (f x), (f (f x)) etc.\n  f must be free of side-effects"}))
(when-let [v (resolve (quote clojure.core/iteration))] (alter-meta! v fill {:arglists (quote ([step & {:keys [somef vf kf initk], :or {vf identity, kf identity, somef some?, initk nil}}])), :doc "Creates a seqable/reducible via repeated calls to step,\n  a function of some (continuation token) 'k'. The first call to step\n  will be passed initk, returning 'ret'. Iff (somef ret) is true,\n  (vf ret) will be included in the iteration, else iteration will\n  terminate and vf/kf will not be called. If (kf ret) is non-nil it\n  will be passed to the next step call, else iteration will terminate.\n\n  This can be used e.g. to consume APIs that return paginated or batched data.\n\n   step - (possibly impure) fn of 'k' -> 'ret'\n\n   :somef - fn of 'ret' -> logical true/false, default 'some?'\n   :vf - fn of 'ret' -> 'v', a value produced by the iteration, default 'identity'\n   :kf - fn of 'ret' -> 'next-k' or nil (signaling 'do not continue'), default 'identity'\n   :initk - the first value passed to step, default 'nil'\n\n  It is presumed that step with non-initk is unreproducible/non-idempotent.\n  If step with initk is unreproducible it is on the consumer to not consume twice."}))
(when-let [v (resolve (quote clojure.core/iterator-seq))] (alter-meta! v fill {:arglists (quote ([iter])), :doc "Returns a seq on a java.util.Iterator. Note that most collections\n  providing iterators implement Iterable and thus support seq directly.\n  Seqs cache values, thus iterator-seq should not be used on any\n  iterator that repeatedly returns the same mutable object."}))
(when-let [v (resolve (quote clojure.core/juxt))] (alter-meta! v fill {:arglists (quote ([f] [f g] [f g h] [f g h & fs])), :doc "Takes a set of functions and returns a fn that is the juxtaposition\n  of those fns.  The returned fn takes a variable number of args, and\n  returns a vector containing the result of applying each fn to the\n  args (left-to-right).\n  ((juxt a b c) x) => [(a x) (b x) (c x)]"}))
(when-let [v (resolve (quote clojure.core/keep))] (alter-meta! v fill {:arglists (quote ([f] [f coll])), :doc "Returns a lazy sequence of the non-nil results of (f item). Note,\n  this means false return values will be included.  f must be free of\n  side-effects.  Returns a transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/keep-indexed))] (alter-meta! v fill {:arglists (quote ([f] [f coll])), :doc "Returns a lazy sequence of the non-nil results of (f index item). Note,\n  this means false return values will be included.  f must be free of\n  side-effects.  Returns a stateful transducer when no collection is\n  provided."}))
(when-let [v (resolve (quote clojure.core/key))] (alter-meta! v fill {:arglists (quote ([e])), :doc "Returns the key of the map entry."}))
(when-let [v (resolve (quote clojure.core/keys))] (alter-meta! v fill {:arglists (quote ([map])), :doc "Returns a sequence of the map's keys, in the same order as (seq map)."}))
(when-let [v (resolve (quote clojure.core/keyword))] (alter-meta! v fill {:arglists (quote ([name] [ns name])), :doc "Returns a Keyword with the given namespace and name.  Do not use :\n  in the keyword strings, it will be added automatically."}))
(when-let [v (resolve (quote clojure.core/keyword?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a Keyword"}))
(when-let [v (resolve (quote clojure.core/last))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Return the last item in coll, in linear time"}))
(when-let [v (resolve (quote clojure.core/lazy-cat))] (alter-meta! v fill {:arglists (quote ([& colls])), :doc "Expands to code which yields a lazy sequence of the concatenation\n  of the supplied colls.  Each coll expr is not evaluated until it is\n  needed. \n\n  (lazy-cat xs ys zs) === (concat (lazy-seq xs) (lazy-seq ys) (lazy-seq zs))"}))
(when-let [v (resolve (quote clojure.core/lazy-seq))] (alter-meta! v fill {:arglists (quote ([& body])), :doc "Takes a body of expressions that returns an ISeq or nil, and yields\n  a Seqable object that will invoke the body only the first time seq\n  is called, and will cache the result and return it on all subsequent\n  seq calls. See also - realized?"}))
(when-let [v (resolve (quote clojure.core/let))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "binding => binding-form init-expr\n  binding-form => name, or destructuring-form\n  destructuring-form => map-destructure-form, or seq-destructure-form\n\n  Evaluates the exprs in a lexical context in which the symbols in\n  the binding-forms are bound to their respective init-exprs or parts\n  therein.\n\n  See https://clojure.org/reference/special_forms#binding-forms for\n  more information about destructuring."}))
(when-let [v (resolve (quote clojure.core/letfn))] (alter-meta! v fill {:arglists (quote ([fnspecs & body])), :doc "fnspec ==> (fname [params*] exprs) or (fname ([params*] exprs)+)\n\n  Takes a vector of function specs and a body, and generates a set of\n  bindings of functions to their names. All of the names are available\n  in all of the definitions of the functions, as well as the body."}))
(when-let [v (resolve (quote clojure.core/line-seq))] (alter-meta! v fill {:arglists (quote ([rdr])), :doc "Returns the lines of text from rdr as a lazy sequence of strings.\n  rdr must implement java.io.BufferedReader."}))
(when-let [v (resolve (quote clojure.core/list))] (alter-meta! v fill {:arglists (quote ([& items])), :doc "Creates a new list containing the items."}))
(when-let [v (resolve (quote clojure.core/list*))] (alter-meta! v fill {:arglists (quote ([args] [a args] [a b args] [a b c args] [a b c d & more])), :doc "Creates a new seq containing the items prepended to the rest, the\n  last of which will be treated as a sequence."}))
(when-let [v (resolve (quote clojure.core/list?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x implements IPersistentList"}))
(when-let [v (resolve (quote clojure.core/load-string))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Sequentially read and evaluate the set of forms contained in the\n  string"}))
(when-let [v (resolve (quote clojure.core/locking))] (alter-meta! v fill {:arglists (quote ([x & body])), :doc "Executes exprs in an implicit do, while holding the monitor of x.\n  Will release the monitor of x in all circumstances."}))
(when-let [v (resolve (quote clojure.core/long))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to long"}))
(when-let [v (resolve (quote clojure.core/long-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of longs"}))
(when-let [v (resolve (quote clojure.core/loop))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "Evaluates the exprs in a lexical context in which the symbols in\n  the binding-forms are bound to their respective init-exprs or parts\n  therein. Acts as a recur target."}))
(when-let [v (resolve (quote clojure.core/macroexpand))] (alter-meta! v fill {:arglists (quote ([form])), :doc "Repeatedly calls macroexpand-1 on form until it no longer\n  represents a macro form, then returns it.  Note neither\n  macroexpand-1 nor macroexpand expand macros in subforms."}))
(when-let [v (resolve (quote clojure.core/macroexpand-1))] (alter-meta! v fill {:arglists (quote ([form])), :doc "If form represents a macro form, returns its expansion,\n  else returns form."}))
(when-let [v (resolve (quote clojure.core/make-array))] (alter-meta! v fill {:arglists (quote ([type len] [type dim & more-dims])), :doc "Creates and returns an array of instances of the specified class of\n  the specified dimension(s).  Note that a class object is required.\n  Class objects can be obtained by using their imported or\n  fully-qualified name.  Class objects for the primitive types can be\n  obtained using, e.g., Integer/TYPE."}))
(when-let [v (resolve (quote clojure.core/make-hierarchy))] (alter-meta! v fill {:arglists (quote ([])), :doc "Creates a hierarchy object for use with derive, isa? etc."}))
(when-let [v (resolve (quote clojure.core/map))] (alter-meta! v fill {:arglists (quote ([f] [f coll] [f c1 c2] [f c1 c2 c3] [f c1 c2 c3 & colls])), :doc "Returns a lazy sequence consisting of the result of applying f to\n  the set of first items of each coll, followed by applying f to the\n  set of second items in each coll, until any one of the colls is\n  exhausted.  Any remaining items in other colls are ignored. Function\n  f should accept number-of-colls arguments. Returns a transducer when\n  no collection is provided."}))
(when-let [v (resolve (quote clojure.core/map-entry?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a map entry"}))
(when-let [v (resolve (quote clojure.core/map-indexed))] (alter-meta! v fill {:arglists (quote ([f] [f coll])), :doc "Returns a lazy sequence consisting of the result of applying f to 0\n  and the first item of coll, followed by applying f to 1 and the second\n  item in coll, etc, until coll is exhausted. Thus function f should\n  accept 2 arguments, index and item. Returns a stateful transducer when\n  no collection is provided."}))
(when-let [v (resolve (quote clojure.core/map?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x implements IPersistentMap"}))
(when-let [v (resolve (quote clojure.core/mapcat))] (alter-meta! v fill {:arglists (quote ([f] [f & colls])), :doc "Returns the result of applying concat to the result of applying map\n  to f and colls.  Thus function f should return a collection. Returns\n  a transducer when no collections are provided"}))
(when-let [v (resolve (quote clojure.core/mapv))] (alter-meta! v fill {:arglists (quote ([f coll] [f c1 c2] [f c1 c2 c3] [f c1 c2 c3 & colls])), :doc "Returns a vector consisting of the result of applying f to the\n  set of first items of each coll, followed by applying f to the set\n  of second items in each coll, until any one of the colls is\n  exhausted.  Any remaining items in other colls are ignored. Function\n  f should accept number-of-colls arguments."}))
(when-let [v (resolve (quote clojure.core/max))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns the greatest of the nums."}))
(when-let [v (resolve (quote clojure.core/max-key))] (alter-meta! v fill {:arglists (quote ([k x] [k x y] [k x y & more])), :doc "Returns the x for which (k x), a number, is greatest.\n\n  If there are multiple such xs, the last one is returned."}))
(when-let [v (resolve (quote clojure.core/memfn))] (alter-meta! v fill {:arglists (quote ([name & args])), :doc "Expands into code that creates a fn that expects to be passed an\n  object and any args and calls the named instance method on the\n  object passing the args. Use when you want to treat a Java method as\n  a first-class fn. name may be type-hinted with the method receiver's\n  type in order to avoid reflective calls."}))
(when-let [v (resolve (quote clojure.core/memoize))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Returns a memoized version of a referentially transparent function. The\n  memoized version of the function keeps a cache of the mapping from arguments\n  to results and, when calls with the same arguments are repeated often, has\n  higher performance at the expense of higher memory use."}))
(when-let [v (resolve (quote clojure.core/merge))] (alter-meta! v fill {:arglists (quote ([& maps])), :doc "Returns a map that consists of the rest of the maps conj-ed onto\n  the first.  If a key occurs in more than one map, the mapping from\n  the latter (left-to-right) will be the mapping in the result."}))
(when-let [v (resolve (quote clojure.core/merge-with))] (alter-meta! v fill {:arglists (quote ([f & maps])), :doc "Returns a map that consists of the rest of the maps conj-ed onto\n  the first.  If a key occurs in more than one map, the mapping(s)\n  from the latter (left-to-right) will be combined with the mapping in\n  the result by calling (f val-in-result val-in-latter)."}))
(when-let [v (resolve (quote clojure.core/meta))] (alter-meta! v fill {:arglists (quote ([obj])), :doc "Returns the metadata of obj, returns nil if there is no metadata."}))
(when-let [v (resolve (quote clojure.core/methods))] (alter-meta! v fill {:arglists (quote ([multifn])), :doc "Given a multimethod, returns a map of dispatch values -> dispatch fns"}))
(when-let [v (resolve (quote clojure.core/min))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Returns the least of the nums."}))
(when-let [v (resolve (quote clojure.core/min-key))] (alter-meta! v fill {:arglists (quote ([k x] [k x y] [k x y & more])), :doc "Returns the x for which (k x), a number, is least.\n\n  If there are multiple such xs, the last one is returned."}))
(when-let [v (resolve (quote clojure.core/mix-collection-hash))] (alter-meta! v fill {:arglists (quote ([hash-basis count])), :doc "Mix final collection hash for ordered or unordered collections.\n   hash-basis is the combined collection hash, count is the number\n   of elements included in the basis. Note this is the hash code\n   consistent with =, different from .hashCode.\n   See http://clojure.org/data_structures#hash for full algorithms."}))
(when-let [v (resolve (quote clojure.core/mod))] (alter-meta! v fill {:arglists (quote ([num div])), :doc "Modulus of num and div. Truncates toward negative infinity."}))
(when-let [v (resolve (quote clojure.core/name))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the name String of a string, symbol or keyword."}))
(when-let [v (resolve (quote clojure.core/namespace))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the namespace String of a symbol or keyword, or nil if not present."}))
(when-let [v (resolve (quote clojure.core/namespace-munge))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Convert a Clojure namespace name to a legal Java package name."}))
(when-let [v (resolve (quote clojure.core/nat-int?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a non-negative fixed precision integer"}))
(when-let [v (resolve (quote clojure.core/neg-int?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a negative fixed precision integer"}))
(when-let [v (resolve (quote clojure.core/neg?))] (alter-meta! v fill {:arglists (quote ([num])), :doc "Returns true if num is less than zero, else false"}))
(when-let [v (resolve (quote clojure.core/newline))] (alter-meta! v fill {:arglists (quote ([])), :doc "Writes a platform-specific newline to *out*"}))
(when-let [v (resolve (quote clojure.core/next))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a seq of the items after the first. Calls seq on its\n  argument.  If there are no more items, returns nil."}))
(when-let [v (resolve (quote clojure.core/nfirst))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Same as (next (first x))"}))
(when-let [v (resolve (quote clojure.core/nil?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is nil, false otherwise."}))
(when-let [v (resolve (quote clojure.core/nnext))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Same as (next (next x))"}))
(when-let [v (resolve (quote clojure.core/not))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is logical false, false otherwise."}))
(when-let [v (resolve (quote clojure.core/not-any?))] (alter-meta! v fill {:arglists (quote ([pred coll])), :doc "Returns false if (pred x) is logical true for any x in coll,\n  else true."}))
(when-let [v (resolve (quote clojure.core/not-empty))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "If coll is empty, returns nil, else coll"}))
(when-let [v (resolve (quote clojure.core/not-every?))] (alter-meta! v fill {:arglists (quote ([pred coll])), :doc "Returns false if (pred x) is logical true for every x in\n  coll, else true."}))
(when-let [v (resolve (quote clojure.core/not=))] (alter-meta! v fill {:arglists (quote ([x] [x y] [x y & more])), :doc "Same as (not (= obj1 obj2))"}))
(when-let [v (resolve (quote clojure.core/ns-aliases))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns a map of the aliases for the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-imports))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns a map of the import mappings for the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-interns))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns a map of the intern mappings for the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-map))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns a map of all the mappings for the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-name))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns the name of the namespace, a symbol."}))
(when-let [v (resolve (quote clojure.core/ns-publics))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns a map of the public intern mappings for the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-refers))] (alter-meta! v fill {:arglists (quote ([ns])), :doc "Returns a map of the refer mappings for the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-resolve))] (alter-meta! v fill {:arglists (quote ([ns sym] [ns env sym])), :doc "Returns the var or Class to which a symbol will be resolved in the\n  namespace (unless found in the environment), else nil.  Note that\n  if the symbol is fully qualified, the var/Class to which it resolves\n  need not be present in the namespace."}))
(when-let [v (resolve (quote clojure.core/ns-unalias))] (alter-meta! v fill {:arglists (quote ([ns sym])), :doc "Removes the alias for the symbol from the namespace."}))
(when-let [v (resolve (quote clojure.core/nth))] (alter-meta! v fill {:arglists (quote ([coll index] [coll index not-found])), :doc "Returns the value at the index. get returns nil if index out of\n  bounds, nth throws an exception unless not-found is supplied.  nth\n  also works for strings, Java arrays, regex Matchers and Lists, and,\n  in O(n) time, for sequences."}))
(when-let [v (resolve (quote clojure.core/nthnext))] (alter-meta! v fill {:arglists (quote ([coll n])), :doc "Returns the nth next of coll, (seq coll) when n is 0."}))
(when-let [v (resolve (quote clojure.core/nthrest))] (alter-meta! v fill {:arglists (quote ([coll n])), :doc "Returns the nth rest of coll, coll when n is 0."}))
(when-let [v (resolve (quote clojure.core/num))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to Number"}))
(when-let [v (resolve (quote clojure.core/number?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is a Number"}))
(when-let [v (resolve (quote clojure.core/numerator))] (alter-meta! v fill {:arglists (quote ([r])), :doc "Returns the numerator part of a Ratio."}))
(when-let [v (resolve (quote clojure.core/object-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq])), :doc "Creates an array of objects"}))
(when-let [v (resolve (quote clojure.core/odd?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is odd, throws an exception if n is not an integer"}))
(when-let [v (resolve (quote clojure.core/or))] (alter-meta! v fill {:arglists (quote ([] [x] [x & next])), :doc "Evaluates exprs one at a time, from left to right. If a form\n  returns a logical true value, or returns that value and doesn't\n  evaluate any of the other expressions, otherwise it returns the\n  value of the last expression. (or) returns nil."}))
(when-let [v (resolve (quote clojure.core/parents))] (alter-meta! v fill {:arglists (quote ([tag] [h tag])), :doc "Returns the immediate parents of tag, either via a Java type\n  inheritance relationship or a relationship established via derive. h\n  must be a hierarchy obtained from make-hierarchy, if not supplied\n  defaults to the global hierarchy"}))
(when-let [v (resolve (quote clojure.core/parse-boolean))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Parse strings \"true\" or \"false\" and return a boolean, or nil if invalid"}))
(when-let [v (resolve (quote clojure.core/parse-double))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Parse string with floating point components and return a Double value,\n  or nil if parse fails.\n\n  Grammar: https://docs.oracle.com/javase/8/docs/api/java/lang/Double.html#valueOf-java.lang.String-"}))
(when-let [v (resolve (quote clojure.core/parse-long))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Parse string of decimal digits with optional leading -/+ and return a\n  Long value, or nil if parse fails"}))
(when-let [v (resolve (quote clojure.core/parse-uuid))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Parse a string representing a UUID and return a java.util.UUID instance,\n  or nil if parse fails.\n\n  Grammar: https://docs.oracle.com/javase/8/docs/api/java/util/UUID.html#toString--"}))
(when-let [v (resolve (quote clojure.core/partial))] (alter-meta! v fill {:arglists (quote ([f] [f arg1] [f arg1 arg2] [f arg1 arg2 arg3] [f arg1 arg2 arg3 & more])), :doc "Takes a function f and fewer than the normal arguments to f, and\n  returns a fn that takes a variable number of additional args. When\n  called, the returned function calls f with args + additional args."}))
(when-let [v (resolve (quote clojure.core/partition))] (alter-meta! v fill {:arglists (quote ([n coll] [n step coll] [n step pad coll])), :doc "Returns a lazy sequence of lists of n items each, at offsets step\n  apart. If step is not supplied, defaults to n, i.e. the partitions\n  do not overlap. If a pad collection is supplied, use its elements as\n  necessary to complete last partition upto n items. In case there are\n  not enough padding elements, return a partition with less than n items."}))
(when-let [v (resolve (quote clojure.core/partition-all))] (alter-meta! v fill {:arglists (quote ([n] [n coll] [n step coll])), :doc "Returns a lazy sequence of lists like partition, but may include\n  partitions with fewer than n items at the end.  Returns a stateful\n  transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/partition-by))] (alter-meta! v fill {:arglists (quote ([f] [f coll])), :doc "Applies f to each value in coll, splitting it each time f returns a\n   new value.  Returns a lazy seq of partitions.  Returns a stateful\n   transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/partitionv))] (alter-meta! v fill {:arglists (quote ([n coll] [n step coll] [n step pad coll])), :doc "Returns a lazy sequence of vectors of n items each, at offsets step\n  apart. If step is not supplied, defaults to n, i.e. the partitions\n  do not overlap. If a pad collection is supplied, use its elements as\n  necessary to complete last partition upto n items. In case there are\n  not enough padding elements, return a partition with less than n items."}))
(when-let [v (resolve (quote clojure.core/partitionv-all))] (alter-meta! v fill {:arglists (quote ([n] [n coll] [n step coll])), :doc "Returns a lazy sequence of vector partitions, but may include\n  partitions with fewer than n items at the end.\n  Returns a stateful transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/pcalls))] (alter-meta! v fill {:arglists (quote ([& fns])), :doc "Executes the no-arg fns in parallel, returning a lazy sequence of\n  their values"}))
(when-let [v (resolve (quote clojure.core/peek))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "For a list or queue, same as first, for a vector, same as, but much\n  more efficient than, last. If the collection is empty, returns nil."}))
(when-let [v (resolve (quote clojure.core/persistent!))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a new, persistent version of the transient collection, in\n  constant time. The transient collection cannot be used after this\n  call, any such use will throw an exception."}))
(when-let [v (resolve (quote clojure.core/pmap))] (alter-meta! v fill {:arglists (quote ([f coll] [f coll & colls])), :doc "Like map, except f is applied in parallel. Semi-lazy in that the\n  parallel computation stays ahead of the consumption, but doesn't\n  realize the entire result unless required. Only useful for\n  computationally intensive functions where the time of f dominates\n  the coordination overhead."}))
(when-let [v (resolve (quote clojure.core/pop))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "For a list or queue, returns a new list/queue without the first\n  item, for a vector, returns a new vector without the last item. If\n  the collection is empty, throws an exception.  Note - not the same\n  as next/butlast."}))
(when-let [v (resolve (quote clojure.core/pop!))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Removes the last item from a transient vector. If\n  the collection is empty, throws an exception. Returns coll"}))
(when-let [v (resolve (quote clojure.core/pop-thread-bindings))] (alter-meta! v fill {:arglists (quote ([])), :doc "Pop one set of bindings pushed with push-binding before. It is an error to\n  pop bindings without pushing before."}))
(when-let [v (resolve (quote clojure.core/pos-int?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a positive fixed precision integer"}))
(when-let [v (resolve (quote clojure.core/pos?))] (alter-meta! v fill {:arglists (quote ([num])), :doc "Returns true if num is greater than zero, else false"}))
(when-let [v (resolve (quote clojure.core/pr))] (alter-meta! v fill {:arglists (quote ([] [x] [x & more])), :doc "Prints the object(s) to the output stream that is the current value\n  of *out*.  Prints the object(s), separated by spaces if there is\n  more than one.  By default, pr and prn print in a way that objects\n  can be read by the reader"}))
(when-let [v (resolve (quote clojure.core/pr-str))] (alter-meta! v fill {:arglists (quote ([& xs])), :doc "pr to a string, returning it"}))
(when-let [v (resolve (quote clojure.core/prefer-method))] (alter-meta! v fill {:arglists (quote ([multifn dispatch-val-x dispatch-val-y])), :doc "Causes the multimethod to prefer matches of dispatch-val-x over dispatch-val-y \n   when there is a conflict"}))
(when-let [v (resolve (quote clojure.core/prefers))] (alter-meta! v fill {:arglists (quote ([multifn])), :doc "Given a multimethod, returns a map of preferred value -> set of other values"}))
(when-let [v (resolve (quote clojure.core/print))] (alter-meta! v fill {:arglists (quote ([& more])), :doc "Prints the object(s) to the output stream that is the current value\n  of *out*.  print and println produce output for human consumption."}))
(when-let [v (resolve (quote clojure.core/print-str))] (alter-meta! v fill {:arglists (quote ([& xs])), :doc "print to a string, returning it"}))
(when-let [v (resolve (quote clojure.core/printf))] (alter-meta! v fill {:arglists (quote ([fmt & args])), :doc "Prints formatted output, as per format"}))
(when-let [v (resolve (quote clojure.core/println))] (alter-meta! v fill {:arglists (quote ([& more])), :doc "Same as print followed by (newline)"}))
(when-let [v (resolve (quote clojure.core/println-str))] (alter-meta! v fill {:arglists (quote ([& xs])), :doc "println to a string, returning it"}))
(when-let [v (resolve (quote clojure.core/prn))] (alter-meta! v fill {:arglists (quote ([& more])), :doc "Same as pr followed by (newline). Observes *flush-on-newline*"}))
(when-let [v (resolve (quote clojure.core/prn-str))] (alter-meta! v fill {:arglists (quote ([& xs])), :doc "prn to a string, returning it"}))
(when-let [v (resolve (quote clojure.core/promise))] (alter-meta! v fill {:arglists (quote ([])), :doc "Returns a promise object that can be read with deref/@, and set,\n  once only, with deliver. Calls to deref/@ prior to delivery will\n  block, unless the variant of deref with timeout is used. All\n  subsequent derefs will return the same delivered value without\n  blocking. See also - realized?."}))
(when-let [v (resolve (quote clojure.core/push-thread-bindings))] (alter-meta! v fill {:arglists (quote ([bindings])), :doc "WARNING: This is a low-level function. Prefer high-level macros like\n  binding where ever possible.\n\n  Takes a map of Var/value pairs. Binds each Var to the associated value for\n  the current thread. Each call *MUST* be accompanied by a matching call to\n  pop-thread-bindings wrapped in a try-finally!\n  \n      (push-thread-bindings bindings)\n      (try\n        ...\n        (finally\n          (pop-thread-bindings)))"}))
(when-let [v (resolve (quote clojure.core/pvalues))] (alter-meta! v fill {:arglists (quote ([& exprs])), :doc "Returns a lazy sequence of the values of the exprs, which are\n  evaluated in parallel"}))
(when-let [v (resolve (quote clojure.core/qualified-ident?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a symbol or keyword with a namespace"}))
(when-let [v (resolve (quote clojure.core/qualified-keyword?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a keyword with a namespace"}))
(when-let [v (resolve (quote clojure.core/qualified-symbol?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a symbol with a namespace"}))
(when-let [v (resolve (quote clojure.core/quot))] (alter-meta! v fill {:arglists (quote ([num div])), :doc "quot[ient] of dividing numerator by denominator."}))
(when-let [v (resolve (quote clojure.core/rand))] (alter-meta! v fill {:arglists (quote ([] [n])), :doc "Returns a random floating point number between 0 (inclusive) and\n  n (default 1) (exclusive)."}))
(when-let [v (resolve (quote clojure.core/rand-int))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns a random integer between 0 (inclusive) and n (exclusive)."}))
(when-let [v (resolve (quote clojure.core/rand-nth))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Return a random element of the (sequential) collection. Will have\n  the same performance characteristics as nth for the given\n  collection."}))
(when-let [v (resolve (quote clojure.core/random-sample))] (alter-meta! v fill {:arglists (quote ([prob] [prob coll])), :doc "Returns items from coll with random probability of prob (0.0 -\n  1.0).  Returns a transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/random-uuid))] (alter-meta! v fill {:arglists (quote ([])), :doc "Returns a pseudo-randomly generated java.util.UUID instance (i.e. type 4).\n\n  See: https://docs.oracle.com/javase/8/docs/api/java/util/UUID.html#randomUUID--"}))
(when-let [v (resolve (quote clojure.core/range))] (alter-meta! v fill {:arglists (quote ([] [end] [start end] [start end step])), :doc "Returns a lazy seq of nums from start (inclusive) to end\n  (exclusive), by step, where start defaults to 0, step to 1, and end to\n  infinity. When step is equal to 0, returns an infinite sequence of\n  start. When start is equal to end, returns empty list."}))
(when-let [v (resolve (quote clojure.core/ratio?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is a Ratio"}))
(when-let [v (resolve (quote clojure.core/rational?))] (alter-meta! v fill {:arglists (quote ([n])), :doc "Returns true if n is a rational number"}))
(when-let [v (resolve (quote clojure.core/rationalize))] (alter-meta! v fill {:arglists (quote ([num])), :doc "returns the rational value of num"}))
(when-let [v (resolve (quote clojure.core/re-find))] (alter-meta! v fill {:arglists (quote ([m] [re s])), :doc "Returns the next regex match, if any, of string to pattern, using\n  java.util.regex.Matcher.find().  Uses re-groups to return the\n  groups."}))
(when-let [v (resolve (quote clojure.core/re-groups))] (alter-meta! v fill {:arglists (quote ([m])), :doc "Returns the groups from the most recent match/find. If there are no\n  nested groups, returns a string of the entire match. If there are\n  nested groups, returns a vector of the groups, the first element\n  being the entire match."}))
(when-let [v (resolve (quote clojure.core/re-matcher))] (alter-meta! v fill {:arglists (quote ([re s])), :doc "Returns an instance of java.util.regex.Matcher, for use, e.g. in\n  re-find."}))
(when-let [v (resolve (quote clojure.core/re-matches))] (alter-meta! v fill {:arglists (quote ([re s])), :doc "Returns the match, if any, of string to pattern, using\n  java.util.regex.Matcher.matches().  Uses re-groups to return the\n  groups."}))
(when-let [v (resolve (quote clojure.core/re-pattern))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Returns an instance of java.util.regex.Pattern, for use, e.g. in\n  re-matcher."}))
(when-let [v (resolve (quote clojure.core/re-seq))] (alter-meta! v fill {:arglists (quote ([re s])), :doc "Returns a lazy sequence of successive matches of pattern in string,\n  using java.util.regex.Matcher.find(), each such match processed with\n  re-groups."}))
(when-let [v (resolve (quote clojure.core/read-line))] (alter-meta! v fill {:arglists (quote ([])), :doc "Reads the next line from stream that is the current value of *in* ."}))
(when-let [v (resolve (quote clojure.core/read-string))] (alter-meta! v fill {:arglists (quote ([s] [opts s])), :doc "Reads one object from the string s. Optionally include reader\n  options, as specified in read.\n\n  Note that read-string can execute code (controlled by *read-eval*),\n  and as such should be used only with trusted sources.\n\n  For data structure interop use clojure.edn/read-string"}))
(when-let [v (resolve (quote clojure.core/realized?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if a value has been produced for a promise, delay, future or lazy sequence."}))
(when-let [v (resolve (quote clojure.core/record?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is a record"}))
(when-let [v (resolve (quote clojure.core/reduce))] (alter-meta! v fill {:arglists (quote ([f coll] [f val coll])), :doc "f should be a function of 2 arguments. If val is not supplied,\n  returns the result of applying f to the first 2 items in coll, then\n  applying f to that result and the 3rd item, etc. If coll contains no\n  items, f must accept no arguments as well, and reduce returns the\n  result of calling f with no arguments.  If coll has only 1 item, it\n  is returned and f is not called.  If val is supplied, returns the\n  result of applying f to val and the first item in coll, then\n  applying f to that result and the 2nd item, etc. If coll contains no\n  items, returns val and f is not called."}))
(when-let [v (resolve (quote clojure.core/reduce-kv))] (alter-meta! v fill {:arglists (quote ([f init coll])), :doc "Reduces an associative collection. f should be a function of 3\n  arguments. Returns the result of applying f to init, the first key\n  and the first value in coll, then applying f to that result and the\n  2nd key and value, etc. If coll contains no entries, returns init\n  and f is not called. Note that reduce-kv is supported on vectors,\n  where the keys will be the ordinals."}))
(when-let [v (resolve (quote clojure.core/reduced))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Wraps x in a way such that a reduce will terminate with the value x"}))
(when-let [v (resolve (quote clojure.core/reduced?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is the result of a call to reduced"}))
(when-let [v (resolve (quote clojure.core/reductions))] (alter-meta! v fill {:arglists (quote ([f coll] [f init coll])), :doc "Returns a lazy seq of the intermediate values of the reduction (as\n  per reduce) of coll by f, starting with init."}))
(when-let [v (resolve (quote clojure.core/ref))] (alter-meta! v fill {:arglists (quote ([x] [x & options])), :doc "Creates and returns a Ref with an initial value of x and zero or\n  more options (in any order):\n\n  :meta metadata-map\n\n  :validator validate-fn\n\n  :min-history (default 0)\n  :max-history (default 10)\n\n  If metadata-map is supplied, it will become the metadata on the\n  ref. validate-fn must be nil or a side-effect-free fn of one\n  argument, which will be passed the intended new state on any state\n  change. If the new state is unacceptable, the validate-fn should\n  return false or throw an exception. validate-fn will be called on\n  transaction commit, when all refs have their final values.\n\n  Normally refs accumulate history dynamically as needed to deal with\n  read demands. If you know in advance you will need history you can\n  set :min-history to ensure it will be available when first needed (instead\n  of after a read fault). History is limited, and the limit can be set\n  with :max-history."}))
(when-let [v (resolve (quote clojure.core/ref-set))] (alter-meta! v fill {:arglists (quote ([ref val])), :doc "Must be called in a transaction. Sets the value of ref.\n  Returns val."}))
(when-let [v (resolve (quote clojure.core/refer))] (alter-meta! v fill {:arglists (quote ([ns-sym & filters])), :doc "refers to all public vars of ns, subject to filters.\n  filters can include at most one each of:\n\n  :exclude list-of-symbols\n  :only list-of-symbols\n  :rename map-of-fromsymbol-tosymbol\n\n  For each public interned var in the namespace named by the symbol,\n  adds a mapping from the name of the var to the var to the current\n  namespace.  Throws an exception if name is already mapped to\n  something else in the current namespace. Filters can be used to\n  select a subset, via inclusion or exclusion, or to provide a mapping\n  to a symbol different from the var's name, in order to prevent\n  clashes. Use :use in the ns macro in preference to calling this directly."}))
(when-let [v (resolve (quote clojure.core/reify))] (alter-meta! v fill {:arglists (quote ([& opts+specs])), :doc "reify creates an object implementing a protocol or interface.\n  reify is a macro with the following structure:\n\n (reify options* specs*)\n  \n  Currently there are no options.\n\n  Each spec consists of the protocol or interface name followed by zero\n  or more method bodies:\n\n  protocol-or-interface-or-Object\n  (methodName [args+] body)*\n\n  Methods should be supplied for all methods of the desired\n  protocol(s) and interface(s). You can also define overrides for\n  methods of Object. Note that the first parameter must be supplied to\n  correspond to the target object ('this' in Java parlance). Thus\n  methods for interfaces will take one more argument than do the\n  interface declarations.  Note also that recur calls to the method\n  head should *not* pass the target object, it will be supplied\n  automatically and can not be substituted.\n\n  The return type can be indicated by a type hint on the method name,\n  and arg types can be indicated by a type hint on arg names. If you\n  leave out all hints, reify will try to match on same name/arity\n  method in the protocol(s)/interface(s) - this is preferred. If you\n  supply any hints at all, no inference is done, so all hints (or\n  default of Object) must be correct, for both arguments and return\n  type. If a method is overloaded in a protocol/interface, multiple\n  independent method definitions must be supplied.  If overloaded with\n  same arity in an interface you must specify complete hints to\n  disambiguate - a missing hint implies Object.\n\n  Method heads are recursion points for recur, as in a fn. The method\n  bodies of reify are lexical closures, and can refer to the surrounding\n  local scope:\n  \n  (str (let [f \"foo\"] \n         (reify Object\n           (toString [this] f))))\n  == \"foo\"\n\n  (seq (let [f \"foo\"] \n         (reify clojure.lang.Seqable\n           (seq [this] (seq f)))))\n  == (\\f \\o \\o)\n  \n  reify always implements clojure.lang.IObj and transfers meta\n  data of the form to the created object.\n  \n  (meta ^{:k :v} (reify Object (toString [this] \"foo\")))\n  == {:k :v}"}))
(when-let [v (resolve (quote clojure.core/release-pending-sends))] (alter-meta! v fill {:arglists (quote ([])), :doc "Normally, actions sent directly or indirectly during another action\n  are held until the action completes (changes the agent's\n  state). This function can be used to dispatch any pending sent\n  actions immediately. This has no impact on actions sent during a\n  transaction, which are still held until commit. If no action is\n  occurring, does nothing. Returns the number of actions dispatched."}))
(when-let [v (resolve (quote clojure.core/rem))] (alter-meta! v fill {:arglists (quote ([num div])), :doc "remainder of dividing numerator by denominator."}))
(when-let [v (resolve (quote clojure.core/remove))] (alter-meta! v fill {:arglists (quote ([pred] [pred coll])), :doc "Returns a lazy sequence of the items in coll for which\n  (pred item) returns logical false. pred must be free of side-effects.\n  Returns a transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/remove-method))] (alter-meta! v fill {:arglists (quote ([multifn dispatch-val])), :doc "Removes the method of multimethod associated with dispatch-value."}))
(when-let [v (resolve (quote clojure.core/remove-tap))] (alter-meta! v fill {:arglists (quote ([f])), :doc "Remove f from the tap set."}))
(when-let [v (resolve (quote clojure.core/remove-watch))] (alter-meta! v fill {:arglists (quote ([reference key])), :doc "Removes a watch (set by add-watch) from a reference"}))
(when-let [v (resolve (quote clojure.core/repeat))] (alter-meta! v fill {:arglists (quote ([x] [n x])), :doc "Returns a lazy (infinite!, or length n if supplied) sequence of xs."}))
(when-let [v (resolve (quote clojure.core/repeatedly))] (alter-meta! v fill {:arglists (quote ([f] [n f])), :doc "Takes a function of no args, presumably with side effects, and\n  returns an infinite (or length n if supplied) lazy sequence of calls\n  to it"}))
(when-let [v (resolve (quote clojure.core/replace))] (alter-meta! v fill {:arglists (quote ([smap] [smap coll])), :doc "Given a map of replacement pairs and a vector/collection, returns a\n  vector/seq with any elements = a key in smap replaced with the\n  corresponding val in smap.  Returns a transducer when no collection\n  is provided."}))
(when-let [v (resolve (quote clojure.core/replicate))] (alter-meta! v fill {:arglists (quote ([n x])), :doc "DEPRECATED: Use 'repeat' instead.\n   Returns a lazy seq of n xs."}))
(when-let [v (resolve (quote clojure.core/require))] (alter-meta! v fill {:arglists (quote ([& args])), :doc "Loads libs, skipping any that are already loaded. Each argument is\n  either a libspec that identifies a lib, a prefix list that identifies\n  multiple libs whose names share a common prefix, or a flag that modifies\n  how all the identified libs are loaded. Use :require in the ns macro\n  in preference to calling this directly.\n\n  Libs\n\n  A 'lib' is a named set of resources in classpath whose contents define a\n  library of Clojure code. Lib names are symbols and each lib is associated\n  with a Clojure namespace and a Java package that share its name. A lib's\n  name also locates its root directory within classpath using Java's\n  package name to classpath-relative path mapping. All resources in a lib\n  should be contained in the directory structure under its root directory.\n  All definitions a lib makes should be in its associated namespace.\n\n  'require loads a lib by loading its root resource. The root resource path\n  is derived from the lib name in the following manner:\n  Consider a lib named by the symbol 'x.y.z; it has the root directory\n  <classpath>/x/y/, and its root resource is <classpath>/x/y/z.clj, or\n  <classpath>/x/y/z.cljc if <classpath>/x/y/z.clj does not exist. The\n  root resource should contain code to create the lib's\n  namespace (usually by using the ns macro) and load any additional\n  lib resources.\n\n  Libspecs\n\n  A libspec is a lib name or a vector containing a lib name followed by\n  options expressed as sequential keywords and arguments.\n\n  Recognized options:\n  :as takes a symbol as its argument and makes that symbol an alias to the\n    lib's namespace in the current namespace.\n  :as-alias takes a symbol as its argument and aliases like :as, however\n    the lib will not be loaded. If the lib has not been loaded, a new\n    empty namespace will be created (as with create-ns).\n  :refer takes a list of symbols to refer from the namespace or the :all\n    keyword to bring in all public vars.\n\n  Prefix Lists\n\n  It's common for Clojure code to depend on several libs whose names have\n  the same prefix. When specifying libs, prefix lists can be used to reduce\n  repetition. A prefix list contains the shared prefix followed by libspecs\n  with the shared prefix removed from the lib names. After removing the\n  prefix, the names that remain must not contain any periods.\n\n  Flags\n\n  A flag is a keyword.\n  Recognized flags: :reload, :reload-all, :verbose\n  :reload forces loading of all the identified libs even if they are\n    already loaded (has no effect on libspecs using :as-alias)\n  :reload-all implies :reload and also forces loading of all libs that the\n    identified libs directly or indirectly load via require or use\n    (has no effect on libspecs using :as-alias)\n  :verbose triggers printing information about each load, alias, and refer\n\n  Example:\n\n  The following would load the libraries clojure.zip and clojure.set\n  abbreviated as 's'.\n\n  (require '(clojure zip [set :as s]))"}))
(when-let [v (resolve (quote clojure.core/requiring-resolve))] (alter-meta! v fill {:arglists (quote ([sym])), :doc "Resolves namespace-qualified sym per 'resolve'. If initial resolve\nfails, attempts to require sym's namespace and retries."}))
(when-let [v (resolve (quote clojure.core/reset!))] (alter-meta! v fill {:arglists (quote ([atom newval])), :doc "Sets the value of atom to newval without regard for the\n  current value. Returns newval."}))
(when-let [v (resolve (quote clojure.core/reset-meta!))] (alter-meta! v fill {:arglists (quote ([iref metadata-map])), :doc "Atomically resets the metadata for a namespace/var/ref/agent/atom"}))
(when-let [v (resolve (quote clojure.core/reset-vals!))] (alter-meta! v fill {:arglists (quote ([atom newval])), :doc "Sets the value of atom to newval. Returns [old new], the value of the\n   atom before and after the reset."}))
(when-let [v (resolve (quote clojure.core/resolve))] (alter-meta! v fill {:arglists (quote ([sym] [env sym])), :doc "same as (ns-resolve *ns* symbol) or (ns-resolve *ns* &env symbol)"}))
(when-let [v (resolve (quote clojure.core/rest))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a possibly empty seq of the items after the first. Calls seq on its\n  argument."}))
(when-let [v (resolve (quote clojure.core/restart-agent))] (alter-meta! v fill {:arglists (quote ([a new-state & options])), :doc "When an agent is failed, changes the agent state to new-state and\n  then un-fails the agent so that sends are allowed again.  If\n  a :clear-actions true option is given, any actions queued on the\n  agent that were being held while it was failed will be discarded,\n  otherwise those held actions will proceed.  The new-state must pass\n  the validator if any, or restart will throw an exception and the\n  agent will remain failed with its old state and error.  Watchers, if\n  any, will NOT be notified of the new state.  Throws an exception if\n  the agent is not failed."}))
(when-let [v (resolve (quote clojure.core/reverse))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a seq of the items in coll in reverse order. Not lazy."}))
(when-let [v (resolve (quote clojure.core/reversible?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns true if coll implements Reversible"}))
(when-let [v (resolve (quote clojure.core/rseq))] (alter-meta! v fill {:arglists (quote ([rev])), :doc "Returns, in constant time, a seq of the items in rev (which\n  can be a vector or sorted-map), in reverse order. If rev is empty returns nil"}))
(when-let [v (resolve (quote clojure.core/rsubseq))] (alter-meta! v fill {:arglists (quote ([sc test key] [sc start-test start-key end-test end-key])), :doc "sc must be a sorted collection, test(s) one of <, <=, > or\n  >=. Returns a reverse seq of those entries with keys ek for\n  which (test (.. sc comparator (compare ek key)) 0) is true"}))
(when-let [v (resolve (quote clojure.core/run!))] (alter-meta! v fill {:arglists (quote ([proc coll])), :doc "Runs the supplied procedure (via reduce), for purposes of side\n  effects, on successive items in the collection. Returns nil"}))
(when-let [v (resolve (quote clojure.core/satisfies?))] (alter-meta! v fill {:arglists (quote ([protocol x])), :doc "Returns true if x satisfies the protocol"}))
(when-let [v (resolve (quote clojure.core/second))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Same as (first (next x))"}))
(when-let [v (resolve (quote clojure.core/select-keys))] (alter-meta! v fill {:arglists (quote ([map keyseq])), :doc "Returns a map containing only those entries in map whose key is in keys"}))
(when-let [v (resolve (quote clojure.core/send))] (alter-meta! v fill {:arglists (quote ([a f & args])), :doc "Dispatch an action to an agent. Returns the agent immediately.\n  Subsequently, in a thread from a thread pool, the state of the agent\n  will be set to the value of:\n\n  (apply action-fn state-of-agent args)"}))
(when-let [v (resolve (quote clojure.core/send-off))] (alter-meta! v fill {:arglists (quote ([a f & args])), :doc "Dispatch a potentially blocking action to an agent. Returns the\n  agent immediately. Subsequently, in a separate thread, the state of\n  the agent will be set to the value of:\n\n  (apply action-fn state-of-agent args)"}))
(when-let [v (resolve (quote clojure.core/send-via))] (alter-meta! v fill {:arglists (quote ([executor a f & args])), :doc "Dispatch an action to an agent. Returns the agent immediately.\n  Subsequently, in a thread supplied by executor, the state of the agent\n  will be set to the value of:\n\n  (apply action-fn state-of-agent args)"}))
(when-let [v (resolve (quote clojure.core/seq))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a seq on the collection. If the collection is\n    empty, returns nil.  (seq nil) returns nil. seq also works on\n    Strings, native Java arrays (of reference types) and any objects\n    that implement Iterable. Note that seqs cache values, thus seq\n    should not be used on any Iterable whose iterator repeatedly\n    returns the same mutable object."}))
(when-let [v (resolve (quote clojure.core/seq-to-map-for-destructuring))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Builds a map from a seq as described in\n  https://clojure.org/reference/special_forms#keyword-arguments"}))
(when-let [v (resolve (quote clojure.core/seq?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x implements ISeq"}))
(when-let [v (resolve (quote clojure.core/seqable?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if the seq function is supported for x"}))
(when-let [v (resolve (quote clojure.core/sequence))] (alter-meta! v fill {:arglists (quote ([coll] [xform coll] [xform coll & colls])), :doc "Coerces coll to a (possibly empty) sequence, if it is not already\n  one. Will not force a lazy seq. (sequence nil) yields (), When a\n  transducer is supplied, returns a lazy sequence of applications of\n  the transform to the items in coll(s), i.e. to the set of first\n  items of each coll, followed by the set of second\n  items in each coll, until any one of the colls is exhausted.  Any\n  remaining items in other colls are ignored. The transform should accept\n  number-of-colls arguments"}))
(when-let [v (resolve (quote clojure.core/sequential?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns true if coll implements Sequential"}))
(when-let [v (resolve (quote clojure.core/set))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a set of the distinct elements of coll."}))
(when-let [v (resolve (quote clojure.core/set-agent-send-executor!))] (alter-meta! v fill {:arglists (quote ([executor])), :doc "Sets the ExecutorService to be used by send"}))
(when-let [v (resolve (quote clojure.core/set-agent-send-off-executor!))] (alter-meta! v fill {:arglists (quote ([executor])), :doc "Sets the ExecutorService to be used by send-off"}))
(when-let [v (resolve (quote clojure.core/set-error-handler!))] (alter-meta! v fill {:arglists (quote ([a handler-fn])), :doc "Sets the error-handler of agent a to handler-fn.  If an action\n  being run by the agent throws an exception or doesn't pass the\n  validator fn, handler-fn will be called with two arguments: the\n  agent and the exception."}))
(when-let [v (resolve (quote clojure.core/set-error-mode!))] (alter-meta! v fill {:arglists (quote ([a mode-keyword])), :doc "Sets the error-mode of agent a to mode-keyword, which must be\n  either :fail or :continue.  If an action being run by the agent\n  throws an exception or doesn't pass the validator fn, an\n  error-handler may be called (see set-error-handler!), after which,\n  if the mode is :continue, the agent will continue as if neither the\n  action that caused the error nor the error itself ever happened.\n  \n  If the mode is :fail, the agent will become failed and will stop\n  accepting new 'send' and 'send-off' actions, and any previously\n  queued actions will be held until a 'restart-agent'.  Deref will\n  still work, returning the state of the agent before the error."}))
(when-let [v (resolve (quote clojure.core/set-validator!))] (alter-meta! v fill {:arglists (quote ([iref validator-fn])), :doc "Sets the validator-fn for a var/ref/agent/atom. validator-fn must be nil or a\n  side-effect-free fn of one argument, which will be passed the intended\n  new state on any state change. If the new state is unacceptable, the\n  validator-fn should return false or throw an exception. If the current state (root\n  value if var) is not acceptable to the new validator, an exception\n  will be thrown and the validator will not be changed."}))
(when-let [v (resolve (quote clojure.core/set?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x implements IPersistentSet"}))
(when-let [v (resolve (quote clojure.core/short))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to short"}))
(when-let [v (resolve (quote clojure.core/short-array))] (alter-meta! v fill {:arglists (quote ([size-or-seq] [size init-val-or-seq])), :doc "Creates an array of shorts"}))
(when-let [v (resolve (quote clojure.core/shuffle))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Return a random permutation of coll"}))
(when-let [v (resolve (quote clojure.core/shutdown-agents))] (alter-meta! v fill {:arglists (quote ([])), :doc "Initiates a shutdown of the thread pools that back the agent\n  system. Running actions will complete, but no new actions will be\n  accepted"}))
(when-let [v (resolve (quote clojure.core/simple-ident?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a symbol or keyword without a namespace"}))
(when-let [v (resolve (quote clojure.core/simple-keyword?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a keyword without a namespace"}))
(when-let [v (resolve (quote clojure.core/simple-symbol?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a symbol without a namespace"}))
(when-let [v (resolve (quote clojure.core/slurp))] (alter-meta! v fill {:arglists (quote ([f & opts])), :doc "Opens a reader on f and reads all its contents, returning a string.\n  See clojure.java.io/reader for a complete list of supported arguments."}))
(when-let [v (resolve (quote clojure.core/some))] (alter-meta! v fill {:arglists (quote ([pred coll])), :doc "Returns the first logical true value of (pred x) for any x in coll,\n  else nil.  One common idiom is to use a set as pred, for example\n  this will return :fred if :fred is in the sequence, otherwise nil:\n  (some #{:fred} coll)"}))
(when-let [v (resolve (quote clojure.core/some->))] (alter-meta! v fill {:arglists (quote ([expr & forms])), :doc "When expr is not nil, threads it into the first form (via ->),\n  and when that result is not nil, through the next etc"}))
(when-let [v (resolve (quote clojure.core/some->>))] (alter-meta! v fill {:arglists (quote ([expr & forms])), :doc "When expr is not nil, threads it into the first form (via ->>),\n  and when that result is not nil, through the next etc"}))
(when-let [v (resolve (quote clojure.core/some-fn))] (alter-meta! v fill {:arglists (quote ([p] [p1 p2] [p1 p2 p3] [p1 p2 p3 & ps])), :doc "Takes a set of predicates and returns a function f that returns the first logical true value\n  returned by one of its composing predicates against any of its arguments, else it returns\n  logical false. Note that f is short-circuiting in that it will stop execution on the first\n  argument that triggers a logical true result against the original predicates."}))
(when-let [v (resolve (quote clojure.core/some?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is not nil, false otherwise."}))
(when-let [v (resolve (quote clojure.core/sort))] (alter-meta! v fill {:arglists (quote ([coll] [comp coll])), :doc "Returns a sorted sequence of the items in coll. If no comparator is\n  supplied, uses compare.  comparator must implement\n  java.util.Comparator.  Guaranteed to be stable: equal elements will\n  not be reordered.  If coll is a Java array, it will be modified.  To\n  avoid this, sort a copy of the array."}))
(when-let [v (resolve (quote clojure.core/sort-by))] (alter-meta! v fill {:arglists (quote ([keyfn coll] [keyfn comp coll])), :doc "Returns a sorted sequence of the items in coll, where the sort\n  order is determined by comparing (keyfn item).  If no comparator is\n  supplied, uses compare.  comparator must implement\n  java.util.Comparator.  Guaranteed to be stable: equal elements will\n  not be reordered.  If coll is a Java array, it will be modified.  To\n  avoid this, sort a copy of the array."}))
(when-let [v (resolve (quote clojure.core/sorted-map))] (alter-meta! v fill {:arglists (quote ([& keyvals])), :doc "keyval => key val\n  Returns a new sorted map with supplied mappings.  If any keys are\n  equal, they are handled as if by repeated uses of assoc."}))
(when-let [v (resolve (quote clojure.core/sorted-map-by))] (alter-meta! v fill {:arglists (quote ([comparator & keyvals])), :doc "keyval => key val\n  Returns a new sorted map with supplied mappings, using the supplied\n  comparator.  If any keys are equal, they are handled as if by\n  repeated uses of assoc."}))
(when-let [v (resolve (quote clojure.core/sorted-set))] (alter-meta! v fill {:arglists (quote ([& keys])), :doc "Returns a new sorted set with supplied keys.  Any equal keys are\n  handled as if by repeated uses of conj."}))
(when-let [v (resolve (quote clojure.core/sorted-set-by))] (alter-meta! v fill {:arglists (quote ([comparator & keys])), :doc "Returns a new sorted set with supplied keys, using the supplied\n  comparator.  Any equal keys are handled as if by repeated uses of\n  conj."}))
(when-let [v (resolve (quote clojure.core/sorted?))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns true if coll implements Sorted"}))
(when-let [v (resolve (quote clojure.core/spit))] (alter-meta! v fill {:arglists (quote ([f content & options])), :doc "Opposite of slurp.  Opens f with writer, writes content, then\n  closes f. Options passed to clojure.java.io/writer."}))
(when-let [v (resolve (quote clojure.core/split-at))] (alter-meta! v fill {:arglists (quote ([n coll])), :doc "Returns a vector of [(take n coll) (drop n coll)]"}))
(when-let [v (resolve (quote clojure.core/split-with))] (alter-meta! v fill {:arglists (quote ([pred coll])), :doc "Returns a vector of [(take-while pred coll) (drop-while pred coll)]"}))
(when-let [v (resolve (quote clojure.core/splitv-at))] (alter-meta! v fill {:arglists (quote ([n coll])), :doc "Returns a vector of [(into [] (take n) coll) (drop n coll)]"}))
(when-let [v (resolve (quote clojure.core/str))] (alter-meta! v fill {:arglists (quote ([] [x] [x & ys])), :doc "With no args, returns the empty string. With one arg x, returns\n  x.toString().  (str nil) returns the empty string. With more than\n  one arg, returns the concatenation of the str values of the args."}))
(when-let [v (resolve (quote clojure.core/string?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a String"}))
(when-let [v (resolve (quote clojure.core/subs))] (alter-meta! v fill {:arglists (quote ([s start] [s start end])), :doc "Returns the substring of s beginning at start inclusive, and ending\n  at end (defaults to length of string), exclusive."}))
(when-let [v (resolve (quote clojure.core/subseq))] (alter-meta! v fill {:arglists (quote ([sc test key] [sc start-test start-key end-test end-key])), :doc "sc must be a sorted collection, test(s) one of <, <=, > or\n  >=. Returns a seq of those entries with keys ek for\n  which (test (.. sc comparator (compare ek key)) 0) is true"}))
(when-let [v (resolve (quote clojure.core/subvec))] (alter-meta! v fill {:arglists (quote ([v start] [v start end])), :doc "Returns a persistent vector of the items in vector from\n  start (inclusive) to end (exclusive).  If end is not supplied,\n  defaults to (count vector). This operation is O(1) and very fast, as\n  the resulting vector shares structure with the original and no\n  trimming is done."}))
(when-let [v (resolve (quote clojure.core/swap!))] (alter-meta! v fill {:arglists (quote ([atom f] [atom f x] [atom f x y] [atom f x y & args])), :doc "Atomically swaps the value of atom to be:\n  (apply f current-value-of-atom args). Note that f may be called\n  multiple times, and thus should be free of side effects.  Returns\n  the value that was swapped in."}))
(when-let [v (resolve (quote clojure.core/swap-vals!))] (alter-meta! v fill {:arglists (quote ([atom f] [atom f x] [atom f x y] [atom f x y & args])), :doc "Atomically swaps the value of atom to be:\n  (apply f current-value-of-atom args). Note that f may be called\n  multiple times, and thus should be free of side effects.\n  Returns [old new], the value of the atom before and after the swap."}))
(when-let [v (resolve (quote clojure.core/symbol))] (alter-meta! v fill {:arglists (quote ([name] [ns name])), :doc "Returns a Symbol with the given namespace and name. Arity-1 works\n  on strings, keywords, and vars."}))
(when-let [v (resolve (quote clojure.core/symbol?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a Symbol"}))
(when-let [v (resolve (quote clojure.core/tagged-literal))] (alter-meta! v fill {:arglists (quote ([tag form])), :doc "Construct a data representation of a tagged literal from a\n  tag symbol and a form."}))
(when-let [v (resolve (quote clojure.core/tagged-literal?))] (alter-meta! v fill {:arglists (quote ([value])), :doc "Return true if the value is the data representation of a tagged literal"}))
(when-let [v (resolve (quote clojure.core/take))] (alter-meta! v fill {:arglists (quote ([n] [n coll])), :doc "Returns a lazy sequence of the first n items in coll, or all items if\n  there are fewer than n.  Returns a stateful transducer when\n  no collection is provided."}))
(when-let [v (resolve (quote clojure.core/take-last))] (alter-meta! v fill {:arglists (quote ([n coll])), :doc "Returns a seq of the last n items in coll.  Depending on the type\n  of coll may be no better than linear time.  For vectors, see also subvec."}))
(when-let [v (resolve (quote clojure.core/take-nth))] (alter-meta! v fill {:arglists (quote ([n] [n coll])), :doc "Returns a lazy seq of every nth item in coll.  Returns a stateful\n  transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/take-while))] (alter-meta! v fill {:arglists (quote ([pred] [pred coll])), :doc "Returns a lazy sequence of successive items from coll while\n  (pred item) returns logical true. pred must be free of side-effects.\n  Returns a transducer when no collection is provided."}))
(when-let [v (resolve (quote clojure.core/tap>))] (alter-meta! v fill {:arglists (quote ([x])), :doc "sends x to any taps. Will not block. Returns true if there was room in the queue,\n  false if not (dropped)."}))
(when-let [v (resolve (quote clojure.core/the-ns))] (alter-meta! v fill {:arglists (quote ([x])), :doc "If passed a namespace, returns it. Else, when passed a symbol,\n  returns the namespace named by it, throwing an exception if not\n  found."}))
(when-let [v (resolve (quote clojure.core/thread-bound?))] (alter-meta! v fill {:arglists (quote ([& vars])), :doc "Returns true if all of the vars provided as arguments have thread-local bindings.\n   Implies that set!'ing the provided vars will succeed.  Returns true if no vars are provided."}))
(when-let [v (resolve (quote clojure.core/time))] (alter-meta! v fill {:arglists (quote ([expr])), :doc "Evaluates expr and prints the time it took.  Returns the value of\n expr."}))
(when-let [v (resolve (quote clojure.core/to-array))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns an array of Objects containing the contents of coll, which\n  can be any Collection.  Maps to java.util.Collection.toArray()."}))
(when-let [v (resolve (quote clojure.core/trampoline))] (alter-meta! v fill {:arglists (quote ([f] [f & args])), :doc "trampoline can be used to convert algorithms requiring mutual\n  recursion without stack consumption. Calls f with supplied args, if\n  any. If f returns a fn, calls that fn with no arguments, and\n  continues to repeat, until the return value is not a fn, then\n  returns that non-fn value. Note that if you want to return a fn as a\n  final value, you must wrap it in some data structure and unpack it\n  after trampoline returns."}))
(when-let [v (resolve (quote clojure.core/transduce))] (alter-meta! v fill {:arglists (quote ([xform f coll] [xform f init coll])), :doc "reduce with a transformation of f (xf). If init is not\n  supplied, (f) will be called to produce it. f should be a reducing\n  step function that accepts both 1 and 2 arguments, if it accepts\n  only 2 you can add the arity-1 with 'completing'. Returns the result\n  of applying (the transformed) xf to init and the first item in coll,\n  then applying xf to that result and the 2nd item, etc. If coll\n  contains no items, returns init and f is not called. Note that\n  certain transforms may inject or skip items."}))
(when-let [v (resolve (quote clojure.core/transient))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Returns a new, transient version of the collection, in constant time.\n\n  Transients support a parallel set of 'changing' operations, with similar names\n  followed by ! - assoc!, conj! etc. These do the same things as their persistent\n  counterparts except the return values are themselves transient.\n\n  Note in particular that transients are not designed to be bashed in-place. You\n  must capture and use the return value in the next call. In this way, they support\n  the same code structure as the functional persistent code they replace."}))
(when-let [v (resolve (quote clojure.core/tree-seq))] (alter-meta! v fill {:arglists (quote ([branch? children root])), :doc "Returns a lazy sequence of the nodes in a tree, via a depth-first walk.\n   branch? must be a fn of one arg that returns true if passed a node\n   that can have children (but may not).  children must be a fn of one\n   arg that returns a sequence of the children. Will only be called on\n   nodes for which branch? returns true. Root is the root node of the\n  tree."}))
(when-let [v (resolve (quote clojure.core/true?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is the value true, false otherwise."}))
(when-let [v (resolve (quote clojure.core/type))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the :type metadata of x, or its Class if none"}))
(when-let [v (resolve (quote clojure.core/unchecked-add))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the sum of x and y, both long.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-add-int))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the sum of x and y, both int.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-byte))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to byte. Subject to rounding or truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-char))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to char. Subject to rounding or truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-dec))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one less than x, a long.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-dec-int))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one less than x, an int.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-divide-int))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the division of x by y, both int.\n  Note - uses a primitive operator subject to truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-double))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to double. Subject to rounding."}))
(when-let [v (resolve (quote clojure.core/unchecked-float))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to float. Subject to rounding."}))
(when-let [v (resolve (quote clojure.core/unchecked-inc))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one greater than x, a long.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-inc-int))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns a number one greater than x, an int.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-int))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to int. Subject to rounding or truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-long))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to long. Subject to rounding or truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-multiply))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the product of x and y, both long.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-multiply-int))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the product of x and y, both int.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-negate))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the negation of x, a long.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-negate-int))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns the negation of x, an int.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-remainder-int))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the remainder of division of x by y, both int.\n  Note - uses a primitive operator subject to truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-short))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce to short. Subject to rounding or truncation."}))
(when-let [v (resolve (quote clojure.core/unchecked-subtract))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the difference of x and y, both long.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/unchecked-subtract-int))] (alter-meta! v fill {:arglists (quote ([x y])), :doc "Returns the difference of x and y, both int.\n  Note - uses a primitive operator subject to overflow."}))
(when-let [v (resolve (quote clojure.core/underive))] (alter-meta! v fill {:arglists (quote ([tag parent] [h tag parent])), :doc "Removes a parent/child relationship between parent and\n  tag. h must be a hierarchy obtained from make-hierarchy, if not\n  supplied defaults to, and modifies, the global hierarchy."}))
(when-let [v (resolve (quote clojure.core/unreduced))] (alter-meta! v fill {:arglists (quote ([x])), :doc "If x is reduced?, returns (deref x), else returns x"}))
(when-let [v (resolve (quote clojure.core/unsigned-bit-shift-right))] (alter-meta! v fill {:arglists (quote ([x n])), :doc "Bitwise shift right, without sign-extension."}))
(when-let [v (resolve (quote clojure.core/update))] (alter-meta! v fill {:arglists (quote ([m k f] [m k f x] [m k f x y] [m k f x y z] [m k f x y z & more])), :doc "'Updates' a value in an associative structure, where k is a\n  key and f is a function that will take the old value\n  and any supplied args and return the new value, and returns a new\n  structure.  If the key does not exist, nil is passed as the old value."}))
(when-let [v (resolve (quote clojure.core/update-in))] (alter-meta! v fill {:arglists (quote ([m ks f & args])), :doc "'Updates' a value in a nested associative structure, where ks is a\n  sequence of keys and f is a function that will take the old value\n  and any supplied args and return the new value, and returns a new\n  nested structure.  If any levels do not exist, hash-maps will be\n  created."}))
(when-let [v (resolve (quote clojure.core/update-keys))] (alter-meta! v fill {:arglists (quote ([m f])), :doc "m f => {(f k) v ...}\n\n  Given a map m and a function f of 1-argument, returns a new map whose\n  keys are the result of applying f to the keys of m, mapped to the\n  corresponding values of m.\n  f must return a unique key for each key of m, else the behavior is undefined."}))
(when-let [v (resolve (quote clojure.core/update-vals))] (alter-meta! v fill {:arglists (quote ([m f])), :doc "m f => {k (f v) ...}\n\n  Given a map m and a function f of 1-argument, returns a new map where the keys of m\n  are mapped to result of applying f to the corresponding values of m."}))
(when-let [v (resolve (quote clojure.core/uri?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a java.net.URI"}))
(when-let [v (resolve (quote clojure.core/use))] (alter-meta! v fill {:arglists (quote ([& args])), :doc "Like 'require, but also refers to each lib's namespace using\n  clojure.core/refer. Use :use in the ns macro in preference to calling\n  this directly.\n\n  'use accepts additional options in libspecs: :exclude, :only, :rename.\n  The arguments and semantics for :exclude, :only, and :rename are the same\n  as those documented for clojure.core/refer."}))
(when-let [v (resolve (quote clojure.core/uuid?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x is a java.util.UUID"}))
(when-let [v (resolve (quote clojure.core/val))] (alter-meta! v fill {:arglists (quote ([e])), :doc "Returns the value in the map entry."}))
(when-let [v (resolve (quote clojure.core/vals))] (alter-meta! v fill {:arglists (quote ([map])), :doc "Returns a sequence of the map's values, in the same order as (seq map)."}))
(when-let [v (resolve (quote clojure.core/var-get))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Gets the value in the var object"}))
(when-let [v (resolve (quote clojure.core/var-set))] (alter-meta! v fill {:arglists (quote ([x val])), :doc "Sets the value in the var object to val. The var must be\n thread-locally bound."}))
(when-let [v (resolve (quote clojure.core/var?))] (alter-meta! v fill {:arglists (quote ([v])), :doc "Returns true if v is of type clojure.lang.Var"}))
(when-let [v (resolve (quote clojure.core/vary-meta))] (alter-meta! v fill {:arglists (quote ([obj f & args])), :doc "Returns an object of the same type and value as obj, with\n  (apply f (meta obj) args) as its metadata."}))
(when-let [v (resolve (quote clojure.core/vec))] (alter-meta! v fill {:arglists (quote ([coll])), :doc "Creates a new vector containing the contents of coll. Java arrays\n  will be aliased and should not be modified."}))
(when-let [v (resolve (quote clojure.core/vector))] (alter-meta! v fill {:arglists (quote ([] [a] [a b] [a b c] [a b c d] [a b c d e] [a b c d e f] [a b c d e f & args])), :doc "Creates a new vector containing the args."}))
(when-let [v (resolve (quote clojure.core/vector?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Return true if x implements IPersistentVector"}))
(when-let [v (resolve (quote clojure.core/volatile!))] (alter-meta! v fill {:arglists (quote ([val])), :doc "Creates and returns a Volatile with an initial value of val."}))
(when-let [v (resolve (quote clojure.core/volatile?))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Returns true if x is a volatile."}))
(when-let [v (resolve (quote clojure.core/vreset!))] (alter-meta! v fill {:arglists (quote ([vol newval])), :doc "Sets the value of volatile to newval without regard for the\n   current value. Returns newval."}))
(when-let [v (resolve (quote clojure.core/vswap!))] (alter-meta! v fill {:arglists (quote ([vol f & args])), :doc "Non-atomically swaps the value of the volatile as if:\n   (apply f current-value-of-vol args). Returns the value that\n   was swapped in."}))
(when-let [v (resolve (quote clojure.core/when))] (alter-meta! v fill {:arglists (quote ([test & body])), :doc "Evaluates test. If logical true, evaluates body in an implicit do."}))
(when-let [v (resolve (quote clojure.core/when-first))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "bindings => x xs\n\n  Roughly the same as (when (seq xs) (let [x (first xs)] body)) but xs is evaluated only once"}))
(when-let [v (resolve (quote clojure.core/when-let))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "bindings => binding-form test\n\n  When test is true, evaluates body with binding-form bound to the value of test"}))
(when-let [v (resolve (quote clojure.core/when-not))] (alter-meta! v fill {:arglists (quote ([test & body])), :doc "Evaluates test. If logical false, evaluates body in an implicit do."}))
(when-let [v (resolve (quote clojure.core/when-some))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "bindings => binding-form test\n\n   When test is not nil, evaluates body with binding-form bound to the\n   value of test"}))
(when-let [v (resolve (quote clojure.core/while))] (alter-meta! v fill {:arglists (quote ([test & body])), :doc "Repeatedly executes body while test expression is true. Presumes\n  some side-effect will cause test to become false/nil. Returns nil"}))
(when-let [v (resolve (quote clojure.core/with-bindings))] (alter-meta! v fill {:arglists (quote ([binding-map & body])), :doc "Takes a map of Var/value pairs. Installs for the given Vars the associated\n  values as thread-local bindings. Then executes body. Pops the installed\n  bindings after body was evaluated. Returns the value of body."}))
(when-let [v (resolve (quote clojure.core/with-bindings*))] (alter-meta! v fill {:arglists (quote ([binding-map f & args])), :doc "Takes a map of Var/value pairs. Installs for the given Vars the associated\n  values as thread-local bindings. Then calls f with the supplied arguments.\n  Pops the installed bindings after f returned. Returns whatever f returns."}))
(when-let [v (resolve (quote clojure.core/with-in-str))] (alter-meta! v fill {:arglists (quote ([s & body])), :doc "Evaluates body in a context in which *in* is bound to a fresh\n  StringReader initialized with the string s."}))
(when-let [v (resolve (quote clojure.core/with-local-vars))] (alter-meta! v fill {:arglists (quote ([name-vals-vec & body])), :doc "varbinding=> symbol init-expr\n\n  Executes the exprs in a context in which the symbols are bound to\n  vars with per-thread bindings to the init-exprs.  The symbols refer\n  to the var objects themselves, and must be accessed with var-get and\n  var-set"}))
(when-let [v (resolve (quote clojure.core/with-meta))] (alter-meta! v fill {:arglists (quote ([obj m])), :doc "Returns an object of the same type and value as obj, with\n    map m as its metadata."}))
(when-let [v (resolve (quote clojure.core/with-open))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "bindings => [name init ...]\n\n  Evaluates body in a try expression with names bound to the values\n  of the inits, and a finally clause that calls (.close name) on each\n  name in reverse order."}))
(when-let [v (resolve (quote clojure.core/with-out-str))] (alter-meta! v fill {:arglists (quote ([& body])), :doc "Evaluates exprs in a context in which *out* is bound to a fresh\n  StringWriter.  Returns the string created by any nested printing\n  calls."}))
(when-let [v (resolve (quote clojure.core/with-precision))] (alter-meta! v fill {:arglists (quote ([precision & exprs])), :doc "Sets the precision and rounding mode to be used for BigDecimal operations.\n\n  Usage: (with-precision 10 (/ 1M 3))\n  or:    (with-precision 10 :rounding HALF_DOWN (/ 1M 3))\n\n  The rounding mode is one of CEILING, FLOOR, HALF_UP, HALF_DOWN,\n  HALF_EVEN, UP, DOWN and UNNECESSARY; it defaults to HALF_UP."}))
(when-let [v (resolve (quote clojure.core/with-redefs))] (alter-meta! v fill {:arglists (quote ([bindings & body])), :doc "binding => var-symbol temp-value-expr\n\n  Temporarily redefines Vars while executing the body.  The\n  temp-value-exprs will be evaluated and each resulting value will\n  replace in parallel the root value of its Var.  After the body is\n  executed, the root values of all the Vars will be set back to their\n  old values.  These temporary changes will be visible in all threads.\n  Useful for mocking out functions during testing."}))
(when-let [v (resolve (quote clojure.core/with-redefs-fn))] (alter-meta! v fill {:arglists (quote ([binding-map func])), :doc "Temporarily redefines Vars during a call to func.  Each val of\n  binding-map will replace the root value of its key which must be\n  a Var.  After func is called with no args, the root values of all\n  the Vars will be set back to their old values.  These temporary\n  changes will be visible in all threads.  Useful for mocking out\n  functions during testing."}))
(when-let [v (resolve (quote clojure.core/xml-seq))] (alter-meta! v fill {:arglists (quote ([root])), :doc "A tree seq on the xml elements as per xml/parse"}))
(when-let [v (resolve (quote clojure.core/zero?))] (alter-meta! v fill {:arglists (quote ([num])), :doc "Returns true if num is zero, else false"}))
(when-let [v (resolve (quote clojure.core/zipmap))] (alter-meta! v fill {:arglists (quote ([keys vals])), :doc "Returns a map with the keys mapped to the corresponding vals."}))
(when-let [v (resolve (quote clojure.string/blank?))] (alter-meta! v fill {:arglists (quote ([s])), :doc "True if s is nil, empty, or contains only whitespace."}))
(when-let [v (resolve (quote clojure.string/capitalize))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Converts first character of the string to upper-case, all other\n  characters to lower-case."}))
(when-let [v (resolve (quote clojure.string/ends-with?))] (alter-meta! v fill {:arglists (quote ([s substr])), :doc "True if s ends with substr."}))
(when-let [v (resolve (quote clojure.string/escape))] (alter-meta! v fill {:arglists (quote ([s cmap])), :doc "Return a new string, using cmap to escape each character ch\n   from s as follows:\n   \n   If (cmap ch) is nil, append ch to the new string.\n   If (cmap ch) is non-nil, append (str (cmap ch)) instead."}))
(when-let [v (resolve (quote clojure.string/includes?))] (alter-meta! v fill {:arglists (quote ([s substr])), :doc "True if s includes substr."}))
(when-let [v (resolve (quote clojure.string/index-of))] (alter-meta! v fill {:arglists (quote ([s value] [s value from-index])), :doc "Return index of value (string or char) in s, optionally searching\n  forward from from-index. Return nil if value not found."}))
(when-let [v (resolve (quote clojure.string/join))] (alter-meta! v fill {:arglists (quote ([coll] [separator coll])), :doc "Returns a string of all elements in coll, as returned by (seq coll),\n   separated by an optional separator."}))
(when-let [v (resolve (quote clojure.string/last-index-of))] (alter-meta! v fill {:arglists (quote ([s value] [s value from-index])), :doc "Return last index of value (string or char) in s, optionally\n  searching backward from from-index. Return nil if value not found."}))
(when-let [v (resolve (quote clojure.string/lower-case))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Converts string to all lower-case."}))
(when-let [v (resolve (quote clojure.string/re-quote-replacement))] (alter-meta! v fill {:arglists (quote ([replacement])), :doc "Given a replacement string that you wish to be a literal\n   replacement for a pattern match in replace or replace-first, do the\n   necessary escaping of special characters in the replacement."}))
(when-let [v (resolve (quote clojure.string/replace))] (alter-meta! v fill {:arglists (quote ([s match replacement])), :doc "Replaces all instance of match with replacement in s.\n\n   match/replacement can be:\n\n   string / string\n   char / char\n   pattern / (string or function of match).\n\n   See also replace-first.\n\n   The replacement is literal (i.e. none of its characters are treated\n   specially) for all cases above except pattern / string.\n\n   For pattern / string, $1, $2, etc. in the replacement string are\n   substituted with the string that matched the corresponding\n   parenthesized group in the pattern.  If you wish your replacement\n   string r to be used literally, use (re-quote-replacement r) as the\n   replacement argument.  See also documentation for\n   java.util.regex.Matcher's appendReplacement method.\n\n   Example:\n   (clojure.string/replace \"Almost Pig Latin\" #\"\\b(\\w)(\\w+)\\b\" \"$2$1ay\")\n   -> \"lmostAay igPay atinLay\""}))
(when-let [v (resolve (quote clojure.string/replace-first))] (alter-meta! v fill {:arglists (quote ([s match replacement])), :doc "Replaces the first instance of match with replacement in s.\n\n   match/replacement can be:\n\n   char / char\n   string / string\n   pattern / (string or function of match).\n\n   See also replace.\n\n   The replacement is literal (i.e. none of its characters are treated\n   specially) for all cases above except pattern / string.\n\n   For pattern / string, $1, $2, etc. in the replacement string are\n   substituted with the string that matched the corresponding\n   parenthesized group in the pattern.  If you wish your replacement\n   string r to be used literally, use (re-quote-replacement r) as the\n   replacement argument.  See also documentation for\n   java.util.regex.Matcher's appendReplacement method.\n\n   Example:\n   (clojure.string/replace-first \"swap first two words\"\n                                 #\"(\\w+)(\\s+)(\\w+)\" \"$3$2$1\")\n   -> \"first swap two words\""}))
(when-let [v (resolve (quote clojure.string/reverse))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Returns s with its characters reversed."}))
(when-let [v (resolve (quote clojure.string/split))] (alter-meta! v fill {:arglists (quote ([s re] [s re limit])), :doc "Splits string on a regular expression.  Optional argument limit is\n  the maximum number of parts. Not lazy. Returns vector of the parts.\n  Trailing empty strings are not returned - pass limit of -1 to return all."}))
(when-let [v (resolve (quote clojure.string/split-lines))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Splits s on \\n or \\r\\n. Trailing empty lines are not returned."}))
(when-let [v (resolve (quote clojure.string/starts-with?))] (alter-meta! v fill {:arglists (quote ([s substr])), :doc "True if s starts with substr."}))
(when-let [v (resolve (quote clojure.string/trim))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Removes whitespace from both ends of string."}))
(when-let [v (resolve (quote clojure.string/trim-newline))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Removes all trailing newline \\n or return \\r characters from\n  string.  Similar to Perl's chomp."}))
(when-let [v (resolve (quote clojure.string/triml))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Removes whitespace from the left side of string."}))
(when-let [v (resolve (quote clojure.string/trimr))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Removes whitespace from the right side of string."}))
(when-let [v (resolve (quote clojure.string/upper-case))] (alter-meta! v fill {:arglists (quote ([s])), :doc "Converts string to all upper-case."}))
(when-let [v (resolve (quote clojure.walk/keywordize-keys))] (alter-meta! v fill {:arglists (quote ([m])), :doc "Recursively transforms all map keys from strings to keywords."}))
(when-let [v (resolve (quote clojure.walk/macroexpand-all))] (alter-meta! v fill {:arglists (quote ([form])), :doc "Recursively performs all possible macroexpansions in form."}))
(when-let [v (resolve (quote clojure.walk/postwalk))] (alter-meta! v fill {:arglists (quote ([f form])), :doc "Performs a depth-first, post-order traversal of form.  Calls f on\n  each sub-form, uses f's return value in place of the original.\n  Recognizes all Clojure data structures. Consumes seqs as with doall."}))
(when-let [v (resolve (quote clojure.walk/postwalk-demo))] (alter-meta! v fill {:arglists (quote ([form])), :doc "Demonstrates the behavior of postwalk by printing each form as it is\n  walked.  Returns form."}))
(when-let [v (resolve (quote clojure.walk/postwalk-replace))] (alter-meta! v fill {:arglists (quote ([smap form])), :doc "Recursively transforms form by replacing keys in smap with their\n  values.  Like clojure/replace but works on any data structure.  Does\n  replacement at the leaves of the tree first."}))
(when-let [v (resolve (quote clojure.walk/prewalk))] (alter-meta! v fill {:arglists (quote ([f form])), :doc "Like postwalk, but does pre-order traversal."}))
(when-let [v (resolve (quote clojure.walk/prewalk-demo))] (alter-meta! v fill {:arglists (quote ([form])), :doc "Demonstrates the behavior of prewalk by printing each form as it is\n  walked.  Returns form."}))
(when-let [v (resolve (quote clojure.walk/prewalk-replace))] (alter-meta! v fill {:arglists (quote ([smap form])), :doc "Recursively transforms form by replacing keys in smap with their\n  values.  Like clojure/replace but works on any data structure.  Does\n  replacement at the root of the tree first."}))
(when-let [v (resolve (quote clojure.walk/stringify-keys))] (alter-meta! v fill {:arglists (quote ([m])), :doc "Recursively transforms all map keys from keywords to strings."}))
(when-let [v (resolve (quote clojure.walk/walk))] (alter-meta! v fill {:arglists (quote ([inner outer form])), :doc "Traverses form, an arbitrary data structure.  inner and outer are\n  functions.  Applies inner to each element of form, building up a\n  data structure of the same type, then applies outer to the result.\n  Recognizes all Clojure data structures. Consumes seqs as with doall."}))
(when-let [v (resolve (quote clojure.edn/read-string))] (alter-meta! v fill {:arglists (quote ([s] [opts s])), :doc "Reads one object from the string s. Returns nil when s is nil or empty.\n\n  Reads data in the edn format (subset of Clojure data):\n  http://edn-format.org\n\n  opts is a map as per clojure.edn/read"}))
(when-let [v (resolve (quote clojure.core.protocols/CollReduce))] (alter-meta! v fill {:doc "Protocol for collection types that can implement reduce faster than\n  first/next recursion. Called by clojure.core/reduce. Baseline\n  implementation defined in terms of Iterable."}))
(when-let [v (resolve (quote clojure.core.protocols/IKVReduce))] (alter-meta! v fill {:doc "Protocol for concrete associative types that can reduce themselves\n   via a function of key and val faster than first/next recursion over map\n   entries. Called by clojure.core/reduce-kv, and has same\n   semantics (just different arg order)."}))
(when-let [v (resolve (quote clojure.core.protocols/coll-reduce))] (alter-meta! v fill {:arglists (quote ([coll f] [coll f val]))}))
(when-let [v (resolve (quote clojure.core.protocols/datafy))] (alter-meta! v fill {:arglists (quote ([o])), :doc "return a representation of o as data (default identity)"}))
(when-let [v (resolve (quote clojure.core.protocols/kv-reduce))] (alter-meta! v fill {:arglists (quote ([amap f init]))}))
(when-let [v (resolve (quote clojure.core.protocols/nav))] (alter-meta! v fill {:arglists (quote ([coll k v])), :doc "return (possibly transformed) v in the context of coll and k (a key/index or nil),\ndefaults to returning v."}))
(when-let [v (resolve (quote clojure.java.io/as-file))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce argument to a file."}))
(when-let [v (resolve (quote clojure.java.io/as-relative-path))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Take an as-file-able thing and return a string if it is\n   a relative path, else IllegalArgumentException."}))
(when-let [v (resolve (quote clojure.java.io/as-url))] (alter-meta! v fill {:arglists (quote ([x])), :doc "Coerce argument to a URL."}))
(when-let [v (resolve (quote clojure.java.io/copy))] (alter-meta! v fill {:arglists (quote ([input output & opts])), :doc "Copies input to output.  Returns nil or throws IOException.\n  Input may be an InputStream, Reader, File, byte[], char[], or String.\n  Output may be an OutputStream, Writer, or File.\n\n  Options are key/value pairs and may be one of\n\n    :buffer-size  buffer size to use, default is 1024.\n    :encoding     encoding to use if converting between\n                  byte and char streams.   \n\n  Does not close any streams except those it opens itself \n  (on a File)."}))
(when-let [v (resolve (quote clojure.java.io/delete-file))] (alter-meta! v fill {:arglists (quote ([f & [silently]])), :doc "Delete file f. If silently is nil or false, raise an exception on failure, else return the value of silently."}))
(when-let [v (resolve (quote clojure.java.io/file))] (alter-meta! v fill {:arglists (quote ([arg] [parent child] [parent child & more])), :doc "Returns a java.io.File, passing each arg to as-file.  Multiple-arg\n   versions treat the first argument as parent and subsequent args as\n   children relative to the parent."}))
(when-let [v (resolve (quote clojure.java.io/input-stream))] (alter-meta! v fill {:arglists (quote ([x & opts])), :doc "Attempts to coerce its argument into an open java.io.InputStream.\n   Default implementations always return a java.io.BufferedInputStream.\n\n   Default implementations are defined for InputStream, File, URI, URL,\n   Socket, byte array, and String arguments.\n\n   If the argument is a String, it tries to resolve it first as a URI, then\n   as a local file name.  URIs with a 'file' protocol are converted to\n   local file names.\n\n   Should be used inside with-open to ensure the InputStream is properly\n   closed."}))
(when-let [v (resolve (quote clojure.java.io/make-parents))] (alter-meta! v fill {:arglists (quote ([f & more])), :doc "Given the same arg(s) as for file, creates all parent directories of\n   the file they represent."}))
(when-let [v (resolve (quote clojure.java.io/output-stream))] (alter-meta! v fill {:arglists (quote ([x & opts])), :doc "Attempts to coerce its argument into an open java.io.OutputStream.\n   Default implementations always return a java.io.BufferedOutputStream.\n\n   Default implementations are defined for OutputStream, File, URI, URL,\n   Socket, and String arguments.\n\n   If the argument is a String, it tries to resolve it first as a URI, then\n   as a local file name.  URIs with a 'file' protocol are converted to\n   local file names.\n\n   Should be used inside with-open to ensure the OutputStream is\n   properly closed."}))
(when-let [v (resolve (quote clojure.java.io/reader))] (alter-meta! v fill {:arglists (quote ([x & opts])), :doc "Attempts to coerce its argument into an open java.io.Reader.\n   Default implementations always return a java.io.BufferedReader.\n\n   Default implementations are provided for Reader, BufferedReader,\n   InputStream, File, URI, URL, Socket, byte arrays, character arrays,\n   and String.\n\n   If argument is a String, it tries to resolve it first as a URI, then\n   as a local file name.  URIs with a 'file' protocol are converted to\n   local file names.\n\n   Should be used inside with-open to ensure the Reader is properly\n   closed."}))
(when-let [v (resolve (quote clojure.java.io/resource))] (alter-meta! v fill {:arglists (quote ([n] [n loader])), :doc "Returns the URL for a named resource. Use the context class loader\n   if no loader is specified."}))
(when-let [v (resolve (quote clojure.java.io/writer))] (alter-meta! v fill {:arglists (quote ([x & opts])), :doc "Attempts to coerce its argument into an open java.io.Writer.\n   Default implementations always return a java.io.BufferedWriter.\n\n   Default implementations are provided for Writer, BufferedWriter,\n   OutputStream, File, URI, URL, Socket, and String.\n\n   If the argument is a String, it tries to resolve it first as a URI, then\n   as a local file name.  URIs with a 'file' protocol are converted to\n   local file names.\n\n   Should be used inside with-open to ensure the Writer is properly\n   closed."}))

;; generated: 628 vars
